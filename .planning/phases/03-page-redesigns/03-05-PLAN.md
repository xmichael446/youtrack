---
phase: 03-page-redesigns
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/contests/ContestListView.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/contests/ContestDetailView.tsx
  - src/features/contests/ContestPlayView.tsx
  - src/features/contests/ContestReviewView.tsx
  - src/features/contests/ContestActionButton.tsx
autonomous: true
requirements: [PAGE-07]
must_haves:
  truths:
    - "Contest list uses Card components for contest items"
    - "Contest cards show status badges (upcoming/active/ended)"
    - "Contest detail view uses Card and Button components"
    - "Contest play view uses Card for questions and Button for answers/submit"
    - "Contest review view uses Card for answer review items"
    - "All contest components import from Phase 2 UI barrel"
  artifacts:
    - path: "src/features/contests/ContestListView.tsx"
      provides: "Contest list with Card and EmptyState components"
      contains: "from '../../components/ui'"
    - path: "src/features/contests/ContestCard.tsx"
      provides: "Contest card with Badge and Button components"
      contains: "from '../../components/ui'"
    - path: "src/features/contests/ContestDetailView.tsx"
      provides: "Contest detail with Card, Button, Badge components"
      contains: "from '../../components/ui'"
    - path: "src/features/contests/ContestPlayView.tsx"
      provides: "Contest play with Card and Button components"
      contains: "from '../../components/ui'"
  key_links:
    - from: "src/features/contests/ContestListView.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
    - from: "src/features/contests/ContestCard.tsx"
      to: "src/components/ui/Badge.tsx"
      via: "import"
      pattern: "import.*Badge.*from.*ui"
    - from: "src/features/contests/ContestDetailView.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import"
      pattern: "import.*Button.*from.*ui"
---

<objective>
Redesign all Contest views (list, card, detail, play, review) and the action button using Phase 2 UI components. Contests have 4 distinct sub-views that need consistent visual treatment.

Purpose: Contests are the competitive gamification feature — they should feel exciting and engaging. Card-based listing with status badges, focused play experience, clear results display.
Output: Six redesigned contest feature files with Phase 2 component integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/03-page-redesigns/03-CONTEXT.md

<interfaces>
From src/components/ui/ barrel export:
```typescript
// Card: variant='default'|'bordered'|'elevated', padding='none'|'sm'|'md'|'lg', hoverable
// Button: variant='primary'|'secondary'|'ghost'|'danger', size='sm'|'md'|'lg', loading, icon, fullWidth
// Badge: variant='brand'|'success'|'warning'|'error'|'muted', size='sm'|'md', dot, pulse
// EmptyState: icon, title, message, action
// Modal: isOpen, onClose, title, subtitle, children, footer, maxWidth
```

import { Button, Card, Badge, EmptyState, Modal } from '../../components/ui';

From src/features/contests/contestTypes.ts:
```typescript
type ContestView = 'list' | 'detail' | 'play' | 'review';
type ContestNav = { view: ContestView; contestId: number | null; answers?: any[] };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ContestListView and ContestCard with Card/Badge/Button</name>
  <files>src/features/contests/ContestListView.tsx, src/features/contests/ContestCard.tsx, src/features/contests/ContestActionButton.tsx</files>
  <read_first>
    - src/features/contests/ContestListView.tsx (list wrapper)
    - src/features/contests/ContestCard.tsx (individual contest card)
    - src/features/contests/ContestActionButton.tsx (action button logic)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/EmptyState.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign ContestListView, ContestCard, and ContestActionButton. Preserve ALL business logic.

CONTESTLISTVIEW.TSX TARGET LAYOUT:
1. Page header: text-h2 with Swords icon + text-body subtitle
2. Refresh button: <Button variant="ghost" size="sm" icon={<RefreshCw />} loading={loading}>
3. Error state: <Card variant="bordered" padding="lg"> with error message + retry <Button variant="secondary" size="sm">
4. Empty state: <EmptyState icon={<Swords />} title={t('contestNoContests')} message={t('contestUpcomingHint')} />
5. Contest list: space-y-3 stack of <ContestCard> items with staggered animation

CONTESTCARD.TSX TARGET LAYOUT:
Read the full file first (8.8K). Then:
1. Card wrapper: <Card variant="default" padding="md" hoverable>
2. Top row: Contest title (text-h3) + status badge (<Badge variant="brand"|"success"|"warning"|"error"> for upcoming/active/ended/registered)
3. Middle: Contest description or details (text-body)
4. Stats row: participant count, duration, question count — each as text-caption items
5. Bottom: <ContestActionButton> (redesigned below)
6. Countdown or "Live Now" indicator if active contest

CONTESTACTIONBUTTON.TSX:
1. Replace all raw buttons with <Button> component:
   - Register: <Button variant="primary" size="md" loading={registering}>
   - View Details: <Button variant="secondary" size="md">
   - Play: <Button variant="primary" size="md">
   - Ended/Results: <Button variant="ghost" size="md">
2. Keep all conditional rendering logic for different contest states

PRESERVE EXACTLY:
- fetchContests, handleRegister in ContestListView
- registeringId state
- toast state and display
- onNavigate callbacks
- All contest status conditional rendering
- Contest data shape and display logic
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/features/contests/ContestListView.tsx src/features/contests/ContestCard.tsx src/features/contests/ContestActionButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ContestListView.tsx imports EmptyState, Button from '../../components/ui'
    - Empty state uses <EmptyState> component
    - Error state uses Card for container, Button for retry
    - ContestCard.tsx imports Card, Badge from '../../components/ui'
    - Contest cards use <Card hoverable> wrapper
    - Status displayed with <Badge> component
    - ContestActionButton.tsx imports Button from '../../components/ui'
    - All action buttons use <Button> component with appropriate variants
    - Registration and navigation logic preserved
  </acceptance_criteria>
  <done>Contest list displays with Card-based items, Badge status indicators, and Button actions. Empty/error states use Phase 2 components. All registration and navigation logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ContestDetailView, ContestPlayView, and ContestReviewView</name>
  <files>src/features/contests/ContestDetailView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestReviewView.tsx</files>
  <read_first>
    - src/features/contests/ContestDetailView.tsx (523 lines — large file, contest detail with leaderboard)
    - src/features/contests/ContestPlayView.tsx (quiz-like play experience)
    - src/features/contests/ContestReviewView.tsx (answer review)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/Modal.tsx
    - .planning/design-system.md (Section 9 — intentional exceptions for silver podium)
  </read_first>
  <action>
Redesign 3 contest sub-views. These are large files — focus on migrating raw HTML elements to Phase 2 components without restructuring business logic.

CONTESTDETAILVIEW.TSX (523 lines) TARGET MIGRATIONS:
Read the full file carefully. Then migrate:
1. Contest info header: <Card variant="elevated" padding="lg">
   - Title: text-h1
   - Description: text-body
   - Status: <Badge variant appropriate to contest state>
   - Stats (participants, duration, questions): text-caption items in flex row
2. Countdown section: <Card variant="default" padding="md"> with countdown timer boxes
3. Contest leaderboard/results: <Card variant="default" padding="none">
   - Podium display: keep existing gold/silver/bronze treatment (Section 9 intentional exception for silver/slate)
   - Rank list rows: styled within Card
4. Action buttons (start, register, etc.): <Button> components
5. Back button usage: keep existing BackButton component
6. DO NOT change the silver podium slate colors (intentional design exception per design-system.md Section 9)

CONTESTPLAYVIEW.TSX TARGET MIGRATIONS:
1. Timer header: <Card variant="elevated" padding="sm"> or prominent bar with countdown
2. Question card: <Card variant="default" padding="lg">
   - Question number: <Badge variant="brand"> or text-label
   - Question text: text-h3 or text-body (depending on length)
3. Answer options: styled as selectable rows within Card
   - Selected state: brand-primary border/background
   - Unselected: bordered, hoverable
4. Navigation: "Previous"/"Next" <Button variant="ghost"|"secondary" size="md">
5. Submit button: <Button variant="primary" size="lg" fullWidth>
6. Progress bar: keep existing but ensure consistent styling

CONTESTREVIEWVIEW.TSX TARGET MIGRATIONS:
1. Results summary: <Card variant="elevated" padding="lg"> with score display
   - Score: text-h1 with color based on result
   - Pass/fail indicator: <Badge variant="success"|"error">
2. Each answer review: <Card variant="bordered" padding="md">
   - Question text: text-body
   - User answer: highlighted with correct/incorrect color
   - Correct answer: shown in green
   - Explanation if available
3. Back to detail: <Button variant="secondary">

PRESERVE IN ALL FILES:
- ContestDetailView: all contest data fetching, leaderboard rendering, navigation, podium logic
- ContestPlayView: timer logic, answer selection, question navigation, submit answers
- ContestReviewView: answer review display, score calculation
- Silver podium styling (intentional exception — keep slate colors)
- onNavigate callbacks
- All useEffect, useState, useCallback hooks
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/features/contests/ContestDetailView.tsx src/features/contests/ContestPlayView.tsx src/features/contests/ContestReviewView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files import from '../../components/ui'
    - ContestDetailView uses Card for info sections, Button for actions, Badge for status
    - Silver podium slate colors PRESERVED (not migrated to semantic tokens)
    - ContestPlayView uses Card for questions, Button for navigation/submit
    - ContestReviewView uses Card for results and answer items, Badge for pass/fail
    - All timer, answer tracking, navigation logic preserved
    - Typography uses design system tokens throughout
  </acceptance_criteria>
  <done>Contest detail, play, and review views use Phase 2 UI components. Card-based layout for all sections. Button-based actions. Badge status indicators. Silver podium exception preserved. All contest flow logic preserved.</done>
</task>

</tasks>

<verification>
Contest list renders with card items. Clicking a contest shows detail view. Contest detail shows info, leaderboard with podium, countdown. Play view shows questions with answer selection. Review view shows score and answer review. Navigation between views works. No console errors.
</verification>

<success_criteria>
- All 6 contest files import from Phase 2 UI barrel
- Card used for all content sections across all views
- Button used for all actions (register, play, submit, navigate)
- Badge used for contest status and quiz results
- Silver podium intentional exception preserved
- All contest flow logic preserved (detail, play, review navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-redesigns/03-05-SUMMARY.md`
</output>
