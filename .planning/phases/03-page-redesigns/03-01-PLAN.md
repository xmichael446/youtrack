---
phase: 03-page-redesigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Login.tsx
  - src/pages/Dashboard.tsx
  - src/components/Curriculum.tsx
  - src/App.tsx
autonomous: true
requirements: [PAGE-01, PAGE-02]
must_haves:
  truths:
    - "Login page uses Button and Input components from Phase 2 UI library"
    - "Login page has brand gradient background with centered card layout"
    - "Dashboard uses Card component for all content sections"
    - "Dashboard stats (streak, rank) use consistent Card layout"
    - "Dashboard enrollment/course section uses Card component"
    - "Both pages use semantic typography tokens (text-h1, text-h2, text-body)"
    - "All page switches apply animate-view-entry animation consistently"
  artifacts:
    - path: "src/pages/Login.tsx"
      provides: "Redesigned login with Phase 2 components"
      contains: "from '../components/ui'"
    - path: "src/pages/Dashboard.tsx"
      provides: "Redesigned dashboard with Phase 2 components"
      contains: "from '../components/ui'"
    - path: "src/App.tsx"
      provides: "Page wrapper with animate-view-entry class"
      contains: "animate-view-entry"
  key_links:
    - from: "src/pages/Login.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import"
      pattern: "import.*Button.*from.*ui"
    - from: "src/pages/Login.tsx"
      to: "src/components/ui/Input.tsx"
      via: "import"
      pattern: "import.*Input.*from.*ui"
    - from: "src/pages/Dashboard.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
---

<objective>
Redesign the Login and Dashboard pages using Phase 2 UI components (Button, Card, Input, Badge) and design system tokens. Ensure consistent page transition animation across all view switches.

Purpose: Login sets the visual first impression; Dashboard is the primary landing page. Together they establish the app's visual identity. Migrating to shared components ensures consistency. The page transition animation (per CONTEXT.md locked decision) ensures every page switch feels cohesive.
Output: Two redesigned page files importing from src/components/ui/ barrel export, plus App.tsx updated with consistent view-entry animation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/03-page-redesigns/03-CONTEXT.md

<interfaces>
<!-- Phase 2 UI component APIs available for use -->

From src/components/ui/Button.tsx:
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  loading?: boolean;
  icon?: React.ReactNode;
  iconPosition?: 'left' | 'right';
  fullWidth?: boolean;
}
// Default export, use: <Button variant="primary" size="lg" fullWidth loading={isLoading}>
```

From src/components/ui/Card.tsx:
```typescript
interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'bordered' | 'elevated';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  hoverable?: boolean;
}
// Default export, use: <Card variant="default" padding="lg">
```

From src/components/ui/Input.tsx:
```typescript
interface InputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  as?: 'input' | 'textarea';
  label?: string;
  helperText?: string;
  error?: string;
  icon?: React.ReactNode;
  size?: 'sm' | 'md' | 'lg';
  textareaRows?: number;
}
// Default export, use: <Input icon={<User />} label="Student Code" error={errorMsg} />
```

From src/components/ui/Badge.tsx:
```typescript
interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'brand' | 'success' | 'warning' | 'error' | 'muted';
  size?: 'sm' | 'md';
  dot?: boolean;
  pulse?: boolean;
}
// Default export, use: <Badge variant="brand" size="sm">Label</Badge>
```

Barrel export: import { Button, Card, Input, Badge, EmptyState } from '../components/ui';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Login page with Phase 2 components</name>
  <files>src/pages/Login.tsx</files>
  <read_first>
    - src/pages/Login.tsx (current implementation to preserve all logic)
    - src/components/ui/Button.tsx (Button API)
    - src/components/ui/Input.tsx (Input API)
    - src/components/ui/Card.tsx (Card API)
    - .planning/design-system.md (token reference)
  </read_first>
  <action>
Redesign Login.tsx visual layer while preserving ALL business logic (studentCode state, authStep, polling, handleSubmit, openTelegramLink, language switching, theme toggle).

TARGET LAYOUT:
1. Full-screen container with brand gradient ambient blobs (keep existing background treatment, it's good)
2. Top controls row: theme toggle + language switcher (keep existing, already clean)
3. Centered Card component (variant="elevated", padding="lg") with max-w-sm on mobile, max-w-4xl on desktop
   - Two-column layout on md+ (flex-row): branding left, auth right
   - Single column on mobile (flex-col)
4. Branding side (left/top): Logo image, text-h1 "Your Space" heading, text-body welcome message with brand-primary highlight
5. Auth side (right/bottom):
   - REPLACE the raw input with <Input icon={<User />} label={t('studentAccess')} size="lg" /> component
   - REPLACE the raw submit button with <Button variant="primary" size="lg" fullWidth loading={isLoading} icon={<LogIn />} iconPosition="right">
   - Keep error display with existing AlertCircle treatment
6. Polling state: keep existing spinner + "Open Telegram" button, but wrap Telegram button in <Button variant="primary" size="lg" fullWidth> with Telegram styling override via className
7. Support link at bottom, Footer component below card

COMPONENT MIGRATION:
- Replace raw <input> element with <Input> component (icon, label, error props)
- Replace raw <button type="submit"> with <Button variant="primary" size="lg" fullWidth loading={isLoading}>
- Wrap main card container in <Card variant="elevated" padding="none"> (padding="none" since internal layout has custom padding)
- Keep all existing Tailwind ambient effects (blurs, gradients, grid texture)

PRESERVE EXACTLY:
- All useState, useEffect, useMutation hooks
- studentCode onChange handler with YT-E prefix logic
- startPolling function
- handleSubmit function
- authStep state machine (login/polling)
- openTelegramLink calls
- isDark/toggleTheme props
- Footer component usage
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/pages/Login.tsx && grep -c "Button\|Input\|Card" src/pages/Login.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Login.tsx imports Button, Input, and/or Card from '../components/ui'
    - Raw <input> for student code replaced with <Input> component
    - Raw submit <button> replaced with <Button variant="primary"> component
    - Main card uses <Card> component or equivalent elevated styling
    - All auth logic preserved: studentCode, authStep, polling, handleSubmit unchanged
    - text-h1 or text-h2 used for page heading
    - text-body used for description text
    - text-caption used for labels/hints
  </acceptance_criteria>
  <done>Login page imports and uses Phase 2 Button and Input components. Visual structure preserved. Auth flow unchanged. Typography uses design system tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Dashboard page with Phase 2 Card component</name>
  <files>src/pages/Dashboard.tsx, src/components/Curriculum.tsx</files>
  <read_first>
    - src/pages/Dashboard.tsx (current implementation to preserve all logic)
    - src/components/Curriculum.tsx (curriculum component, check if it needs Card migration)
    - src/components/ui/Card.tsx (Card API)
    - src/components/ui/Button.tsx (Button API)
    - src/components/ui/Badge.tsx (Badge API)
    - .planning/design-system.md (token reference)
  </read_first>
  <action>
Redesign Dashboard.tsx visual layer while preserving ALL business logic (user/course/event data, timeLeft countdown, RankBadge helper, navigateTo calls, openExternalLink).

TARGET LAYOUT (keep the existing excellent section structure, migrate to components):
1. Hero section: Wrap competitive hero card in <Card variant="default" padding="lg">
   - Greeting: text-caption brand-primary, text-h1 user name, streak/rank prompt
   - Stats grid (2-col): streak icon+count, rank icon+count with RankBadge — keep existing layout
   - Motivating summary paragraph: text-body
2. Event card (right column on lg): Wrap in <Card variant="default" padding="md">
   - Status badge: use <Badge variant="brand"|"success"> for event type indicator
   - Event title: text-h4
   - Countdown grid: keep existing 3-column countdown boxes
   - Active event CTA: use <Button variant="primary" size="md" fullWidth> for "Mark Attendance"
3. Course info strip: Wrap in <Card variant="bordered" padding="none">
   - Image area + content area (existing flex layout is good)
   - Section label, text-h3 course name, text-body description
   - Teacher pills: keep existing styling
4. Tutorial/video banner: Wrap in <Card padding="lg"> with brand-dark background override via className
   - Use <Button variant="primary"> for "Watch Tutorial" CTA
5. Curriculum section: leave Curriculum component as-is (it may need its own migration pass)

COMPONENT MIGRATION:
- Replace raw div cards with <Card> components throughout
- Replace raw <button> CTAs with <Button> components
- Use <Badge> for event status indicators (live/upcoming)
- Keep all existing animation classes (animate-in, fade-in, duration-*)

PRESERVE EXACTLY:
- All React hooks and effects (useEffect for scroll, countdown timer)
- RankBadge inline component
- useDashboard/useLanguage/useNavigation context usage
- LoadingScreen conditional rendering
- Event conditional rendering (event && ...)
- openExternalLink for tutorial video
- BASE_URL usage for images
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/pages/Dashboard.tsx && grep -c "<Card\|<Button\|<Badge" src/pages/Dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx imports Card (and optionally Button, Badge) from '../components/ui'
    - Hero section wrapped in <Card> component
    - Event card wrapped in <Card> component
    - Course info section wrapped in <Card> component
    - At least one <Button> component used (tutorial CTA or attendance CTA)
    - All countdown logic preserved
    - All context hooks unchanged (useDashboard, useLanguage, useNavigation)
    - Typography uses text-h1, text-h2, text-h3, text-body, text-caption tokens
  </acceptance_criteria>
  <done>Dashboard page uses Card components for all sections, Button for CTAs. Visual hierarchy maintained. All data fetching and countdown logic preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Apply consistent view-entry animation to all page switches</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (find the page wrapper div with key={currentView})
    - index.html (confirm animate-view-entry keyframe exists in Tailwind config)
  </read_first>
  <action>
Per locked decision in CONTEXT.md: "shared view-entry animation (fade-in + translateY) applied consistently to all page switches."

In App.tsx, locate the page content wrapper div (currently `<div key={currentView} className="w-full">` around line 224 in AppContent). Add the `animate-view-entry` class to this div so that every page switch triggers the fade-in + translateY animation automatically via React's key-based remount.

SPECIFIC CHANGE:
- Find: `<div key={currentView} className="w-full">`
- Change to: `<div key={currentView} className="w-full animate-view-entry">`

This single change ensures ALL pages (Dashboard, Leaderboard, Lessons, Rewards, Contests, Profile) get the same entry animation on every view switch, because React remounts the div when `key` changes, re-triggering the CSS animation.

VERIFY the `animate-view-entry` animation is defined in index.html Tailwind config (it was added in Phase 1). If not present, do NOT create it here — flag it as a blocker.

PRESERVE EXACTLY:
- All other App.tsx logic (routing, auth, providers, header, footer, nav)
- The key={currentView} prop (essential for animation trigger)
- All existing className values on the div
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep "animate-view-entry" src/App.tsx && grep "animate-view-entry" index.html</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx page wrapper div includes `animate-view-entry` class
    - The animation triggers on every page switch (key={currentView} causes remount)
    - The animate-view-entry keyframe exists in index.html Tailwind config
    - No other App.tsx logic changed
  </acceptance_criteria>
  <done>All page switches consistently apply the animate-view-entry animation via the shared wrapper div in App.tsx. Every page gets the same fade-in + translateY transition.</done>
</task>

</tasks>

<verification>
All pages render correctly with `npm run dev`. Login flow works end-to-end (code entry, form submit). Dashboard shows user data, countdown, course info, curriculum. Page transitions animate on every view switch. No console errors. Phase 2 components imported from barrel export.
</verification>

<success_criteria>
- Login uses Button + Input components from Phase 2 library
- Dashboard uses Card + Button components from Phase 2 library
- Both pages use design system typography (text-h1/h2/h3/body/caption)
- All business logic preserved unchanged
- Visual quality matches or exceeds current implementation
- All page switches trigger animate-view-entry animation consistently
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-redesigns/03-01-SUMMARY.md`
</output>
