---
phase: 03-page-redesigns
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Profile.tsx
  - src/features/profile/ProfileHero.tsx
  - src/features/profile/AchievementShowcase.tsx
  - src/features/profile/ActivityHeatmap.tsx
  - src/features/profile/ActivityFeed.tsx
  - src/features/profile/ProfileEdit.tsx
  - src/features/profile/PrivacySettings.tsx
  - src/components/Header.tsx
autonomous: true
requirements: [PAGE-06, PAGE-08]
must_haves:
  truths:
    - "Profile page uses Card components for hero and content sections"
    - "Profile feature components use Button and Badge from Phase 2"
    - "Notification dropdown in Header uses Card and Badge components"
    - "Notification items have clear read/unread visual distinction"
    - "Notifications are grouped by date (Today/Yesterday/Earlier)"
    - "Profile hero section displays avatar, name, and stats prominently"
  artifacts:
    - path: "src/features/profile/ProfileHero.tsx"
      provides: "Profile hero with Card and Badge components"
      contains: "from '../../components/ui'"
    - path: "src/components/Header.tsx"
      provides: "Notification dropdown with Phase 2 components and date grouping"
      contains: "from '../components/ui'"
  key_links:
    - from: "src/features/profile/ProfileHero.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
    - from: "src/components/Header.tsx"
      to: "src/components/ui/Badge.tsx"
      via: "import"
      pattern: "import.*Badge.*from.*ui"
---

<objective>
Redesign the Profile page (hero + all feature sub-components) and the Notification dropdown in Header using Phase 2 UI components. Notifications grouped by date per CONTEXT.md decision.

Purpose: Profile is the personal identity page — needs to feel engaging and show achievements. Notifications need clear type badges, read/unread states, and date grouping. Both tie into the user identity system.
Output: Eight redesigned files with Phase 2 component integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/03-page-redesigns/03-CONTEXT.md

<interfaces>
From src/components/ui/ barrel export:
```typescript
// Card: variant='default'|'bordered'|'elevated', padding='none'|'sm'|'md'|'lg', hoverable
// Button: variant='primary'|'secondary'|'ghost'|'danger', size='sm'|'md'|'lg', loading, icon, fullWidth
// Badge: variant='brand'|'success'|'warning'|'error'|'muted', size='sm'|'md', dot, pulse
// Input: as='input'|'textarea', label, helperText, error, icon, size
// EmptyState: icon, title, message, action
// Modal: isOpen, onClose, title, subtitle, children, footer, maxWidth
```

import { Button, Card, Badge, Input, EmptyState } from '../components/ui';  // pages
import { Button, Card, Badge, Input, EmptyState } from '../../components/ui';  // features
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Profile.tsx wrapper and ProfileHero component</name>
  <files>src/pages/Profile.tsx, src/features/profile/ProfileHero.tsx</files>
  <read_first>
    - src/pages/Profile.tsx (wrapper with sub-view routing)
    - src/features/profile/ProfileHero.tsx (hero header with avatar + stats)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign Profile.tsx and ProfileHero.tsx visual layer. Preserve ALL business logic.

PROFILE.TSX:
1. Replace the inline Toast component with import from Phase 2 Toast
2. Keep subView state machine (view/edit/settings) and all data fetching logic
3. Keep loadProfile, loadMoreActivity, handleSave, handlePrivacyUpdate functions

PROFILEHERO.TSX TARGET LAYOUT:
1. Hero container: <Card variant="default" padding="lg"> with relative positioning for background accent
2. Top section: Large avatar (w-20 h-20 rounded-pill) centered or left-aligned
   - Avatar with border ring treatment
   - Name: text-h1
   - Student code: text-caption muted
   - Level badge: <Badge variant="brand"> with level icon
3. Stats row: 3-4 stat items in flex row
   - XP: icon + count with text-h3 number
   - Coins: icon + count with text-h3 number
   - Streak: flame icon + days with text-h3 number
   - Rank: award icon + rank number
   - Each stat in a mini card or bordered section
4. Action buttons (own profile): Edit profile <Button variant="secondary" size="sm">, Settings <Button variant="ghost" size="sm">
5. Action buttons (other profile): no edit/settings

PRESERVE EXACTLY:
- All state management in Profile.tsx (subView, profile, loading, error, toast)
- Profile API calls (getProfile, getProfileView, etc.)
- is_own_profile checks for showing/hiding edit controls
- loadProfile, loadMoreActivity, handleSave, handlePrivacyUpdate functions
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/pages/Profile.tsx src/features/profile/ProfileHero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Profile.tsx uses Toast from Phase 2 (or keeps inline if necessary)
    - ProfileHero.tsx imports Card, Button, Badge from '../../components/ui'
    - Hero section uses Card with avatar, name, stats display
    - Stats row displays XP, coins, streak, rank prominently
    - Action buttons use Button component with appropriate variants
    - is_own_profile conditional rendering preserved
  </acceptance_criteria>
  <done>Profile.tsx and ProfileHero.tsx redesigned with Phase 2 components. Hero displays avatar + stats in Card. Action buttons use Button. All profile logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign profile sub-components (achievements, activity, edit, privacy)</name>
  <files>src/features/profile/AchievementShowcase.tsx, src/features/profile/ActivityHeatmap.tsx, src/features/profile/ActivityFeed.tsx, src/features/profile/ProfileEdit.tsx, src/features/profile/PrivacySettings.tsx</files>
  <read_first>
    - src/features/profile/AchievementShowcase.tsx (achievement display)
    - src/features/profile/ActivityHeatmap.tsx (GitHub-style heatmap)
    - src/features/profile/ActivityFeed.tsx (activity list)
    - src/features/profile/ProfileEdit.tsx (edit form)
    - src/features/profile/PrivacySettings.tsx (privacy toggles)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/Input.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign 5 profile feature sub-components. Preserve ALL business logic.

ACHIEVEMENTSHOWCASE.TSX:
1. Section container: <Card variant="bordered" padding="md">
2. Section header: text-h3 "Achievements"
3. Achievement grid: flex-wrap row of achievement items
4. Each achievement: <Badge> or small <Card padding="sm"> with icon + label
5. Locked achievements: muted/opacity treatment

ACTIVITYHEATMAP.TSX:
1. Wrap in <Card variant="default" padding="md">
2. Keep the heatmap grid rendering (it's custom SVG/div-based)
3. Legend: text-caption labels
4. Month labels: text-caption

ACTIVITYFEED.TSX:
1. Wrap in <Card variant="default" padding="none"> (list has its own padding)
2. Each activity item: padded row with icon + description + timestamp
3. Activity type icon: color-coded per type
4. "Load more" button: <Button variant="ghost" size="sm">

PROFILEEDIT.TSX:
1. Wrap form in <Card variant="default" padding="lg">
2. All text inputs: <Input label={...} error={...} />
3. Avatar upload: keep existing but style button as <Button variant="secondary" size="sm">
4. Save button: <Button variant="primary" fullWidth loading={saving}>
5. Cancel button: <Button variant="ghost">

PRIVACYSETTINGS.TSX:
1. Wrap in <Card variant="default" padding="lg">
2. Each privacy toggle: labeled row with switch/toggle
3. Save button: <Button variant="primary" loading={saving}>
4. Description text: text-body for explanations

PRESERVE IN ALL FILES:
- All state management (heatmap data, activity list, form state, privacy state)
- Activity pagination (loadMoreActivity)
- Profile edit save logic
- Privacy update logic
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/features/profile/AchievementShowcase.tsx src/features/profile/ActivityFeed.tsx src/features/profile/ProfileEdit.tsx src/features/profile/PrivacySettings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AchievementShowcase uses Card for container, Badge for achievements
    - ActivityHeatmap wrapped in Card
    - ActivityFeed wrapped in Card, "Load more" uses Button
    - ProfileEdit uses Input components for form fields, Button for actions
    - PrivacySettings uses Card container and Button for save
    - All data fetching and save logic preserved
  </acceptance_criteria>
  <done>All 5 profile sub-components use Phase 2 UI components. Achievement, activity, edit, and privacy sections all use consistent Card/Button/Badge/Input. All logic preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Notification dropdown in Header with Badge, Card, and date grouping</name>
  <files>src/components/Header.tsx</files>
  <read_first>
    - src/components/Header.tsx (full file — notification dropdown is inline)
    - src/components/ui/Card.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/EmptyState.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign the notification dropdown panel inside Header.tsx. The notification panel is rendered inline in Header (not a separate page). Preserve ALL Header functionality.

NOTIFICATION DROPDOWN TARGET LAYOUT:
1. Dropdown container: <Card variant="elevated" padding="none"> replacing raw div
   - Keep existing absolute positioning, width, z-index, animate-in
2. Header row: "Notifications" text-h4 + "Mark all read" <Button variant="ghost" size="sm">
3. DATE GROUPING (per CONTEXT.md locked decision: "grouped by date"):
   - Group notifications into sections: "Today", "Yesterday", "Earlier"
   - Add a grouping function that categorizes notifications by their timestamp:
     - Today: notification date matches current date
     - Yesterday: notification date matches current date minus 1
     - Earlier: everything else
   - Render each group with a section divider: `<div className="px-4 py-2 text-caption text-secondary bg-surface-secondary dark:bg-dark-surface-secondary">{groupLabel}</div>`
   - Only render group headers for groups that have notifications
   - Within each group, maintain chronological order (newest first)
4. Each notification item:
   - Type badge: <Badge variant="brand"|"success"|"muted" size="sm"> for lesson_times/hw_approved/default
   - Timestamp: text-caption muted, tabular-nums
   - Message header: text-body with appropriate color
   - Message body: text-caption secondary
   - Unread indicator: left border brand-primary (4px) or subtle background tint
5. Empty state: <EmptyState icon={<Bell />} message={t('noNotifications')} /> replacing raw empty div

DO NOT CHANGE:
- Header layout, logo, stats cluster, profile menu
- All useState, useEffect, useRef hooks
- showNotifications toggle logic
- markAllAsRead function calls
- getNotificationTypeInfo function (keep or adapt to Badge variants)
- formatTime function
- Z_INDEX usage
- All profile menu logic (showProfileMenu, avatar, logout, theme, language)
- useNotifications hook usage

ONLY MODIFY the notification dropdown section (the showNotifications && ... block) and its supporting functions/rendering. Add the date grouping helper function within Header.tsx (small utility, not worth extracting).
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/components/Header.tsx && grep -c "<Card\|<Badge\|<Button\|<EmptyState" src/components/Header.tsx && grep -c "Today\|Yesterday\|Earlier" src/components/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx imports Card, Badge, EmptyState (and optionally Button) from '../components/ui'
    - Notification dropdown panel uses Card variant="elevated"
    - Notification type labels use Badge component
    - Notifications are grouped by date with "Today", "Yesterday", "Earlier" section dividers
    - Empty notification state uses EmptyState component
    - Read/unread visual distinction preserved or enhanced
    - All Header non-notification functionality unchanged
    - Profile menu, stats, logo, theme toggle all preserved exactly
  </acceptance_criteria>
  <done>Notification dropdown uses Card for container, Badge for type labels, EmptyState for empty list. Notifications grouped by Today/Yesterday/Earlier with section dividers. Read/unread states clearly distinguished. All Header functionality preserved.</done>
</task>

</tasks>

<verification>
Profile page renders correctly with hero, achievements, activity. Edit form works. Privacy settings save. Notification dropdown opens/closes from bell icon. Notifications grouped by date (Today/Yesterday/Earlier). Notification types display with badges. Mark all read works. No console errors.
</verification>

<success_criteria>
- Profile hero uses Card + Badge for avatar/stats display
- Profile feature components use Phase 2 Input, Button, Card
- Notification dropdown uses Card + Badge + EmptyState
- Notifications grouped by date with section dividers
- Read/unread notification states visually clear
- All profile data fetching and editing preserved
- All Header functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-redesigns/03-04-SUMMARY.md`
</output>
