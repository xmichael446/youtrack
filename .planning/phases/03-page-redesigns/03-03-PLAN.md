---
phase: 03-page-redesigns
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/lessons/LessonsContent.tsx
  - src/features/lessons/ActiveAttendanceCard.tsx
  - src/features/lessons/CurrentAssignmentSection.tsx
  - src/features/lessons/AssignmentHistoryCard.tsx
  - src/features/lessons/QuizSection.tsx
  - src/features/lessons/SubmissionModal.tsx
autonomous: true
requirements: [PAGE-04]
must_haves:
  truths:
    - "Lessons page uses Card components for attendance and assignment sections"
    - "Lessons page uses Button components for all interactive actions"
    - "Assignment submission modal uses Modal component from Phase 2"
    - "Quiz section uses Card and Button components"
    - "All lesson feature components import from Phase 2 UI barrel"
  artifacts:
    - path: "src/features/lessons/LessonsContent.tsx"
      provides: "Redesigned lessons wrapper with Phase 2 components"
      contains: "from '../../components/ui'"
    - path: "src/features/lessons/ActiveAttendanceCard.tsx"
      provides: "Attendance card using Phase 2 Card/Button"
      contains: "from '../../components/ui'"
    - path: "src/features/lessons/QuizSection.tsx"
      provides: "Quiz section using Phase 2 Card/Button"
      contains: "from '../../components/ui'"
  key_links:
    - from: "src/features/lessons/LessonsContent.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
    - from: "src/features/lessons/SubmissionModal.tsx"
      to: "src/components/ui/Modal.tsx"
      via: "import"
      pattern: "import.*Modal.*from.*ui"
---

<objective>
Redesign the Lessons page and all its feature sub-components using Phase 2 UI components. The Lessons page has the most sub-components (attendance, assignments, quiz, submission modal) and needs consistent Card/Button/Badge migration.

Purpose: Lessons is a high-engagement page where students mark attendance, submit assignments, and take quizzes. Consistent component usage makes the complex flow feel coherent.
Output: Six redesigned feature component files with Phase 2 component integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/03-page-redesigns/03-CONTEXT.md

<interfaces>
From src/components/ui/ barrel export:
```typescript
// Card: variant='default'|'bordered'|'elevated', padding='none'|'sm'|'md'|'lg', hoverable
// Button: variant='primary'|'secondary'|'ghost'|'danger', size='sm'|'md'|'lg', loading, icon, fullWidth
// Badge: variant='brand'|'success'|'warning'|'error'|'muted', size='sm'|'md', dot, pulse
// Input: as='input'|'textarea', label, helperText, error, icon, size='sm'|'md'|'lg'
// EmptyState: icon, title, message, action
// Modal: isOpen, onClose, title, subtitle, children, footer, maxWidth='sm'|'md'|'lg'
// Toast: already used in LessonsContent
```

import { Button, Card, Badge, Input, EmptyState, Modal } from '../../components/ui';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign LessonsContent wrapper and ActiveAttendanceCard</name>
  <files>src/features/lessons/LessonsContent.tsx, src/features/lessons/ActiveAttendanceCard.tsx</files>
  <read_first>
    - src/features/lessons/LessonsContent.tsx (wrapper with sections)
    - src/features/lessons/ActiveAttendanceCard.tsx (attendance marking UI)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/EmptyState.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign LessonsContent.tsx and ActiveAttendanceCard.tsx visual layer. Preserve ALL business logic.

LESSONCONTENT.TSX TARGET LAYOUT:
1. Page title: text-h2 with BookOpen icon + text-body subtitle (keep existing)
2. Attendance section:
   - Section header: icon + text-h4 label with brand-primary accent
   - Active attendance: <ActiveAttendanceCard> (redesigned below)
   - No attendance: <EmptyState icon={<BookOpen />} message={t('noLessonsSubtitle')} /> replacing the raw empty div
3. Assignments section:
   - Section header: icon + text-h4 label
   - Current assignment: <CurrentAssignmentSection> (redesigned in Task 2)
   - History label: text-label with HistoryIcon
   - Previous assignments: <AssignmentHistoryCard> list (redesigned in Task 2)
   - No assignments: <EmptyState icon={<ClipboardList />} message={t('noAssignmentsSubtitle')} />
4. SubmissionModal: keep existing usage

COMPONENT MIGRATION for LessonsContent:
- Replace raw empty state divs with <EmptyState> component
- Section headers: consistent pattern (icon + text-h4)
- Keep Toast usage as-is (already using Phase 2 Toast)

ACTIVEATTENDANCECARD.TSX TARGET LAYOUT:
Read the full file first. Then:
1. Wrap the entire card in <Card variant="default" padding="md">
2. Status header row: lesson number <Badge variant="brand">, attendance status <Badge variant="success"|"warning"|"error"|"muted">
3. Lesson info: text-h3 topic name, text-body lesson description if available
4. Time remaining: countdown or deadline display using text-caption
5. Action area:
   - Mark attendance button: <Button variant="primary" size="md" loading={marking}>
   - Already marked indicator: <Badge variant="success"> with check icon
6. Quiz link/teaser if quiz data exists: use <Button variant="secondary" size="sm">

PRESERVE EXACTLY:
- All useState, useLessons hooks in LessonsContent
- toggleRow function, expandedRowIds state
- toast state and showToast function
- All attendance marking logic in ActiveAttendanceCard
- Quiz navigation/link logic
- isAttendanceOpen, hasMarkedAttendance computed values
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/features/lessons/LessonsContent.tsx src/features/lessons/ActiveAttendanceCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LessonsContent.tsx imports EmptyState from '../../components/ui'
    - Empty states replaced with <EmptyState> component
    - ActiveAttendanceCard.tsx imports Card, Button, Badge from '../../components/ui'
    - Attendance card wrapped in <Card> component
    - Mark attendance button uses <Button variant="primary">
    - Attendance status uses <Badge> component
    - All attendance marking logic preserved
  </acceptance_criteria>
  <done>LessonsContent uses EmptyState for empty sections. ActiveAttendanceCard uses Card, Button, Badge. All attendance flow preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign assignment cards, quiz section, and submission modal</name>
  <files>src/features/lessons/CurrentAssignmentSection.tsx, src/features/lessons/AssignmentHistoryCard.tsx, src/features/lessons/QuizSection.tsx, src/features/lessons/SubmissionModal.tsx</files>
  <read_first>
    - src/features/lessons/CurrentAssignmentSection.tsx (current assignment display)
    - src/features/lessons/AssignmentHistoryCard.tsx (expandable history cards)
    - src/features/lessons/QuizSection.tsx (quiz flow — large file, focus on UI elements)
    - src/features/lessons/SubmissionModal.tsx (assignment submission modal)
    - src/components/ui/Card.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Badge.tsx
    - src/components/ui/Input.tsx
    - src/components/ui/Modal.tsx
    - .planning/design-system.md
  </read_first>
  <action>
Redesign 4 lesson feature components. Preserve ALL business logic. These files are large — focus on migrating raw HTML elements to Phase 2 components, not restructuring layout.

CURRENTASSIGNMENTSECTION.TSX:
1. Wrap section in <Card variant="default" padding="md">
2. Assignment status: use <Badge variant="success"|"warning"|"error"> for status indicators
3. Assignment title: text-h3
4. Due date/deadline: text-caption with status color
5. Submit button: <Button variant="primary" size="md"> with appropriate icon
6. File attachment display: keep existing but use text-body for filenames
7. Description: text-body

ASSIGNMENTHISTORYCARD.TSX:
1. Each history card: <Card variant="bordered" padding="sm" hoverable> (clickable to expand)
2. Collapsed row: assignment name (text-h4), status badge (<Badge>), grade/score
3. Expanded content: assignment details, feedback, attachments
4. Resubmit button if applicable: <Button variant="secondary" size="sm">

QUIZSECTION.TSX (large file — 33K):
This is the most complex component. Focus on these UI migrations:
1. Quiz intro card: <Card variant="elevated" padding="lg"> with quiz info
2. Start quiz button: <Button variant="primary" size="lg">
3. Question cards: <Card variant="default" padding="md"> per question
4. Answer options: styled divs with hover states (keep existing radio/checkbox patterns)
5. Submit quiz button: <Button variant="primary" size="lg">
6. Quiz results card: <Card variant="elevated"> with score display
7. Review warning: <Card variant="bordered"> with warning text about permanent review penalty
8. Answer review items: <Card padding="sm"> per reviewed answer
9. Status badges throughout: <Badge> components

SUBMISSIONMODAL.TSX:
1. Replace the raw modal wrapper with <Modal isOpen={isOpen} onClose={onClose} title={title} maxWidth="md">
2. File input area: keep existing drag-and-drop, but use text-body/text-caption for labels
3. Text input: <Input as="textarea" label={...} /> for submission text
4. Link input: <Input label="Link" icon={<ExternalLink />} /> for URL submissions
5. Submit button: <Button variant="primary" loading={submitting} fullWidth>
6. Cancel button: <Button variant="ghost">

PRESERVE IN ALL FILES:
- All useState, useEffect, useCallback hooks
- All API calls and data fetching
- All event handlers (submit, expand, navigate)
- Quiz timer logic
- Quiz review warning logic (permanent disqualification concern)
- File upload logic in SubmissionModal
- Assignment grading display logic
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/features/lessons/CurrentAssignmentSection.tsx src/features/lessons/AssignmentHistoryCard.tsx src/features/lessons/QuizSection.tsx src/features/lessons/SubmissionModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files import from '../../components/ui'
    - CurrentAssignmentSection uses Card and Button components
    - AssignmentHistoryCard uses Card (hoverable) and Badge components
    - QuizSection uses Card and Button for quiz flow steps
    - SubmissionModal uses Modal component wrapper instead of raw portal/overlay
    - All quiz logic preserved (timer, review warning, answer tracking)
    - All submission logic preserved (file upload, text, link inputs)
  </acceptance_criteria>
  <done>All 4 lesson feature components migrated to Phase 2 UI components. Quiz flow, assignment submission, and history display all use consistent Card/Button/Badge/Modal. All business logic preserved.</done>
</task>

</tasks>

<verification>
Lessons page renders correctly. Attendance marking works. Assignments display with expand/collapse. Quiz flow works (start, answer, submit, review). Submission modal opens/closes and accepts file/text/link input. No console errors.
</verification>

<success_criteria>
- All 6 lesson-related files import from Phase 2 UI barrel
- Card used for all content sections
- Button used for all actions
- Badge used for all status indicators
- Modal used for submission dialog
- EmptyState used for empty sections
- Quiz flow preserved including review warning
- Assignment submission preserved including file upload
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-redesigns/03-03-SUMMARY.md`
</output>
