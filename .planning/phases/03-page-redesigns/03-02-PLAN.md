---
phase: 03-page-redesigns
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Leaderboard.tsx
  - src/pages/Rewards.tsx
  - src/components/CoinsHistory.tsx
autonomous: true
requirements: [PAGE-03, PAGE-05]
must_haves:
  truths:
    - "Leaderboard uses Card components for podium and rank list"
    - "Leaderboard top-3 podium has gold/silver/bronze visual treatment using Badge or styled Cards"
    - "Rewards page uses Card components for reward items"
    - "Rewards balance header is visually prominent"
    - "Both pages import from Phase 2 UI barrel export"
  artifacts:
    - path: "src/pages/Leaderboard.tsx"
      provides: "Redesigned leaderboard with Card and Badge components"
      contains: "from '../components/ui'"
    - path: "src/pages/Rewards.tsx"
      provides: "Redesigned rewards shop with Card and Button components"
      contains: "from '../components/ui'"
  key_links:
    - from: "src/pages/Leaderboard.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
    - from: "src/pages/Rewards.tsx"
      to: "src/components/ui/Card.tsx"
      via: "import"
      pattern: "import.*Card.*from.*ui"
    - from: "src/pages/Rewards.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import"
      pattern: "import.*Button.*from.*ui"
---

<objective>
Redesign the Leaderboard and Rewards pages using Phase 2 UI components. These are the core gamification pages — leaderboard should feel competitive and exciting, rewards should feel like a shop.

Purpose: Both pages are gamification-heavy and need engaging visuals that reward students. Consistent Card/Button/Badge usage ties them to the rest of the app.
Output: Two redesigned page files with Phase 2 component integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/03-page-redesigns/03-CONTEXT.md

<interfaces>
From src/components/ui/Card.tsx:
```typescript
interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'bordered' | 'elevated';
  padding?: 'none' | 'sm' | 'md' | 'lg';
  hoverable?: boolean;
}
```

From src/components/ui/Button.tsx:
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger';
  size?: 'sm' | 'md' | 'lg';
  loading?: boolean;
  icon?: React.ReactNode;
  iconPosition?: 'left' | 'right';
  fullWidth?: boolean;
}
```

From src/components/ui/Badge.tsx:
```typescript
interface BadgeProps extends React.HTMLAttributes<HTMLSpanElement> {
  variant?: 'brand' | 'success' | 'warning' | 'error' | 'muted';
  size?: 'sm' | 'md';
  dot?: boolean;
  pulse?: boolean;
}
```

From src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon?: React.ReactNode;
  title?: string;
  message: string;
  action?: React.ReactNode;
  className?: string;
}
```

Barrel export: import { Button, Card, Badge, EmptyState } from '../components/ui';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Leaderboard page with podium and Card components</name>
  <files>src/pages/Leaderboard.tsx</files>
  <read_first>
    - src/pages/Leaderboard.tsx (full file — preserve all ranking logic)
    - src/components/ui/Card.tsx (Card API)
    - src/components/ui/Badge.tsx (Badge API)
    - src/components/ui/EmptyState.tsx (EmptyState API)
    - .planning/design-system.md (token reference)
  </read_first>
  <action>
Redesign Leaderboard.tsx visual layer while preserving ALL business logic (activeTab, groupLeaderboard/courseLeaderboard, currentRank, renderRankChangeInline, getRankBadge, navigateToProfile).

TARGET LAYOUT:
1. Page header: text-h2 with Trophy icon, text-body subtitle
2. Tab switcher (group/course): Two pill buttons using <Button variant="secondary"|"ghost" size="sm"> in a flex row with gap-2, active tab gets variant="primary" or branded background. Wrap in a rounded-pill container.
3. Current user rank banner: <Card variant="bordered" padding="sm"> showing "Your Rank" with rank number, XP, rank change indicator. Use <Badge variant="brand"> for rank display.
4. Top-3 podium section: Three <Card hoverable> components arranged horizontally:
   - Center (1st place): Taller card, gold gradient top border or gold bg-amber accent, Crown icon, larger avatar, text-h3 name, XP amount
   - Left (2nd place): Medium height, silver (slate) accent, rank "2" badge
   - Right (3rd place): Medium height, bronze (orange) accent, rank "3" badge
   - Keep existing getRankBadge visual treatment (gold/silver/bronze gradients) — it's already great
5. Remaining ranks (4+): Each rank as a row inside <Card padding="sm"> or as list items within a single <Card>:
   - Rank badge (left), avatar + name (center), XP + rank change (right)
   - Clickable rows that call navigateToProfile
   - Highlight current user's row with brand-primary/5 background
6. Loading state: keep LoadingScreen
7. Empty state: use <EmptyState icon={<Trophy />} message={t('noRankings')} />

COMPONENT MIGRATION:
- Tab buttons: <Button variant="ghost"|"primary" size="sm"> for group/course toggle
- Rank list container: <Card variant="default" padding="none"> wrapping all rank rows
- Each rank row: styled div inside Card (not separate Cards per row — too heavy)
- Current user banner: <Card variant="bordered">
- Top-3 podium cards: <Card hoverable padding="md"> with gradient accents via className
- Empty state: <EmptyState> component

PRESERVE EXACTLY:
- useState for activeTab
- useLeaderboard, useDashboard, useNavigation hooks
- getRankBadge function logic
- renderRankChangeInline function
- navigateToProfile click handlers
- LeaderboardProvider wrapper in parent (Leaderboard.tsx is actually LeaderboardContent)
- All avatar rendering logic
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/pages/Leaderboard.tsx && grep -c "<Card\|<Button\|<Badge\|<EmptyState" src/pages/Leaderboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Leaderboard.tsx imports Card, Button, Badge from '../components/ui'
    - Tab switcher uses Button components for group/course toggle
    - Rank list rows wrapped in Card component(s)
    - Top-3 podium uses Card components with gold/silver/bronze visual accents
    - getRankBadge and renderRankChangeInline logic preserved
    - navigateToProfile click handlers preserved
    - Typography uses text-h2, text-body, text-caption tokens
  </acceptance_criteria>
  <done>Leaderboard displays top-3 podium with Card components and gold/silver/bronze visuals. Rank list uses consistent Card layout. Tab switching works. All ranking data preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Rewards/Shop page with Card and Button components</name>
  <files>src/pages/Rewards.tsx, src/components/CoinsHistory.tsx</files>
  <read_first>
    - src/pages/Rewards.tsx (full file — preserve all claiming logic)
    - src/components/CoinsHistory.tsx (coins history component, check if needs migration)
    - src/components/ui/Card.tsx (Card API)
    - src/components/ui/Button.tsx (Button API)
    - src/components/ui/Badge.tsx (Badge API)
    - src/components/ui/EmptyState.tsx (EmptyState API)
    - src/components/ui/Modal.tsx (Modal API for claim confirmation)
    - .planning/design-system.md (token reference)
  </read_first>
  <action>
Redesign Rewards.tsx visual layer while preserving ALL business logic (sortedRewards, sortedLevelRewards, handleClaim, handleClaimLevel, processingId, balance logic, openTelegramLink).

TARGET LAYOUT:
1. Page header: text-h2 with ShoppingBag icon, text-body subtitle
2. Tab row: Balance rewards / Level rewards / History tabs using <Button variant="ghost"|"primary" size="sm">
3. Balance header card: <Card variant="elevated" padding="lg"> showing current coin balance prominently
   - Large coin count: text-h1 with Coins icon, amber color
   - Subtitle: "Available to spend" text-body
4. Balance reward grid: 2-column grid on mobile, 3-column on md+
   - Each reward: <Card hoverable padding="md">
     - Reward image (if available) or placeholder icon at top
     - Reward name: text-h4
     - Cost: <Badge variant="brand"> showing coin cost
     - Claim button: <Button variant="primary" size="sm" loading={processingId === reward.id}> for claimable
     - Claimed state: <Button variant="secondary" size="sm"> with check icon
     - Locked state: <Button variant="ghost" size="sm" disabled> with Lock icon
     - Affordability indicator: green border or brand highlight if user can afford
5. Level rewards section: Similar card grid
   - Each level reward: <Card hoverable padding="md">
     - Level badge/number display
     - Reward description
     - Unlock status: <Badge variant="success"|"muted">
     - Claim/locked button treatment matching balance rewards
6. Coins history: keep CoinsHistory component, migrate if it has raw buttons/cards
7. Empty state: <EmptyState icon={<Gift />} message={t('noRewards')} />

COMPONENT MIGRATION:
- All reward cards: <Card hoverable padding="md">
- All claim/action buttons: <Button> with appropriate variant
- Status badges: <Badge variant="success"|"warning"|"muted">
- Balance display card: <Card variant="elevated">
- Empty states: <EmptyState> component

PRESERVE EXACTLY:
- useState for processingId, active tab
- useShop, useDashboard hooks
- sortedRewards and sortedLevelRewards sorting logic
- handleClaim and handleClaimLevel async functions
- openTelegramLink for claimed rewards
- ShopProvider wrapper in parent
- Loading/error conditional rendering
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "from.*components/ui" src/pages/Rewards.tsx && grep -c "<Card\|<Button\|<Badge" src/pages/Rewards.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Rewards.tsx imports Card, Button, Badge from '../components/ui'
    - Reward items displayed in Card components
    - Claim buttons use Button component with loading state
    - Balance display uses Card variant="elevated"
    - Status indicators use Badge component
    - All claiming logic preserved (handleClaim, handleClaimLevel, processingId)
    - Typography uses text-h1 for balance, text-h4 for reward names, text-body for descriptions
  </acceptance_criteria>
  <done>Rewards page displays as a shop with Card-based reward items, Button-based claim actions, and Badge status indicators. Balance prominently displayed. All claiming logic preserved.</done>
</task>

</tasks>

<verification>
Both pages render correctly with `npm run dev`. Leaderboard shows rankings with podium. Tab switching works. Rewards shows reward grid with correct affordability states. Claiming flow works. No console errors.
</verification>

<success_criteria>
- Leaderboard uses Card/Button/Badge components from Phase 2 library
- Rewards uses Card/Button/Badge components from Phase 2 library
- Top-3 podium feels celebratory with gold/silver/bronze accents
- Rewards feels like a shop with product-style cards
- All business logic preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-redesigns/03-02-SUMMARY.md`
</output>
