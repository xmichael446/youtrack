---
phase: 04-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/pages/Leaderboard.tsx
  - src/pages/Rewards.tsx
  - src/pages/Login.tsx
  - src/pages/Profile.tsx
  - src/pages/Lessons.tsx
  - src/pages/Contests.tsx
  - src/features/contests/ContestDetailView.tsx
  - src/features/contests/ContestPlayView.tsx
  - src/features/contests/ContestReviewView.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/lessons/LessonsContent.tsx
  - src/features/lessons/QuizSection.tsx
  - src/features/lessons/ActiveAttendanceCard.tsx
  - src/features/lessons/AssignmentHistoryCard.tsx
  - src/features/lessons/CurrentAssignmentSection.tsx
  - src/features/profile/ProfileHero.tsx
  - src/features/profile/AchievementShowcase.tsx
  - src/features/profile/ActivityHeatmap.tsx
  - src/components/Header.tsx
  - src/components/Curriculum.tsx
  - src/components/CoinsHistory.tsx
  - src/App.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "No horizontal scroll at 375px viewport width on any page"
    - "No content clipped or hidden at 375px"
    - "All grids collapse to single column at 375px"
    - "Text does not overflow containers at 375px"
    - "Interactive elements remain tappable (min 44px touch target) at 375px"
  artifacts:
    - path: "src/pages/Dashboard.tsx"
      provides: "Responsive dashboard grid"
      contains: "grid-cols-1"
    - path: "src/pages/Rewards.tsx"
      provides: "Responsive reward grid"
      contains: "grid-cols-1"
    - path: "src/pages/Leaderboard.tsx"
      provides: "Responsive leaderboard layout"
      contains: "overflow"
  key_links:
    - from: "all page files"
      to: "375px viewport"
      via: "Tailwind responsive classes"
      pattern: "md:|grid-cols-1|truncate|overflow"
---

<objective>
Ensure every page is fully usable at 375px viewport width with no horizontal scrolling, no clipped content, and no broken layouts.

Purpose: The app runs inside Telegram Mini App where 375px is the minimum viewport. Every layout must work perfectly at this width (per UX-01). The current codebase has responsive patterns (md: breakpoints) but needs a systematic audit for overflow-causing patterns.

Output: All page and feature component files updated with responsive fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix responsive issues in pages (Dashboard, Leaderboard, Rewards, Login, Profile, Lessons, Contests)</name>
  <files>src/pages/Dashboard.tsx, src/pages/Leaderboard.tsx, src/pages/Rewards.tsx, src/pages/Login.tsx, src/pages/Profile.tsx, src/pages/Lessons.tsx, src/pages/Contests.tsx</files>
  <read_first>src/pages/Dashboard.tsx, src/pages/Leaderboard.tsx, src/pages/Rewards.tsx, src/pages/Login.tsx, src/pages/Profile.tsx, src/pages/Lessons.tsx, src/pages/Contests.tsx</read_first>
  <action>
  Systematically audit and fix each page file for 375px responsiveness. The two-tier approach: base styles for 375px, md: (768px) for desktop.

  COMMON OVERFLOW PATTERNS TO FIX:
  1. Fixed-width containers (w-[NNNpx]) without max-w or responsive alternatives
  2. Horizontal flex layouts that don't wrap at small widths (add flex-wrap or stack vertically)
  3. Grid layouts with too many columns at base (ensure grid-cols-1 at base, md:grid-cols-N for desktop)
  4. Text that overflows (add truncate or break-words where appropriate)
  5. Padding/margin that eats too much horizontal space at 375px (reduce px-6/px-8 to px-4 at base)

  PER-PAGE FIXES:

  Dashboard.tsx:
  - Line ~102: `grid grid-cols-1 ${event ? 'lg:grid-cols-3' : ''}` -- change lg: to md: for earlier breakpoint
  - Line ~233: `grid grid-cols-3` -- verify this is okay at 375px (it's stats, should be fine if items are small). If items overflow, change to `grid grid-cols-3 gap-1` with smaller gap
  - Check all card content: ensure long text (course names, descriptions) uses `truncate` or `line-clamp-2`
  - Ensure page padding is px-4 at base (not larger)

  Leaderboard.tsx:
  - Large file (20.8K) -- read carefully for any horizontal layouts
  - Podium section: if using absolute positioning or fixed widths, ensure they scale down
  - Ranking list: ensure it's single-column and scrollable vertically
  - Check tab bar: if horizontal tabs exist, ensure they fit at 375px or use scrollable overflow-x-auto

  Rewards.tsx:
  - Line ~209: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3` -- this is correct pattern, verify
  - Check reward card content: prices, descriptions should not overflow
  - Check point balance display: ensure large numbers don't overflow

  Login.tsx:
  - Check the card width: ensure max-w constraints work at 375px (should be w-full with max-w)
  - Check Telegram deep link button: ensure it doesn't overflow
  - Padding: ensure horizontal padding is px-4 at base

  Profile.tsx:
  - Check stats row: if horizontal flex, ensure it wraps or fits at 375px
  - Check avatar/name section: ensure it stacks properly

  Lessons.tsx:
  - Thin wrapper (280 bytes) -- likely delegates to LessonsContent. Just verify.

  Contests.tsx:
  - Check contest list cards: ensure they're full-width at 375px

  RESPONSIVE RULE: For all pages, ensure the outermost content container uses `px-4` at base (not px-6 or larger). Use `md:px-6` for desktop.
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && for f in src/pages/Dashboard.tsx src/pages/Leaderboard.tsx src/pages/Rewards.tsx src/pages/Login.tsx src/pages/Profile.tsx; do echo "=== $f ===" && grep -n "grid-cols-[2-9]" "$f" | grep -v "md:\|lg:\|grid-cols-1" || echo "OK: no unresponsive grids"; done</automated>
  </verify>
  <acceptance_criteria>
  - No grid-cols-N (N>1) at base breakpoint without md: or lg: prefix (except grid-cols-3 for small stat items that fit at 375px)
  - All pages use px-4 at base for horizontal padding
  - Long text in cards uses truncate or line-clamp
  - No fixed-width containers wider than 375px at base breakpoint
  </acceptance_criteria>
  <done>All 7 page files audited and fixed for 375px responsiveness</done>
</task>

<task type="auto">
  <name>Task 2: Fix responsive issues in feature components and shell</name>
  <files>src/features/contests/ContestDetailView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestReviewView.tsx, src/features/contests/ContestCard.tsx, src/features/lessons/LessonsContent.tsx, src/features/lessons/QuizSection.tsx, src/features/lessons/ActiveAttendanceCard.tsx, src/features/lessons/AssignmentHistoryCard.tsx, src/features/lessons/CurrentAssignmentSection.tsx, src/features/profile/ProfileHero.tsx, src/features/profile/AchievementShowcase.tsx, src/features/profile/ActivityHeatmap.tsx, src/components/Header.tsx, src/components/Curriculum.tsx, src/components/CoinsHistory.tsx, src/App.tsx</files>
  <read_first>src/features/contests/ContestDetailView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestReviewView.tsx, src/features/lessons/LessonsContent.tsx, src/features/lessons/QuizSection.tsx, src/features/lessons/ActiveAttendanceCard.tsx, src/features/lessons/AssignmentHistoryCard.tsx, src/features/lessons/CurrentAssignmentSection.tsx, src/features/profile/ProfileHero.tsx, src/features/profile/AchievementShowcase.tsx, src/features/profile/ActivityHeatmap.tsx, src/components/Header.tsx, src/components/Curriculum.tsx, src/components/CoinsHistory.tsx, src/App.tsx</read_first>
  <action>
  Continue the 375px audit for feature components and shell:

  CONTEST FEATURES:
  ContestDetailView.tsx:
  - Podium layout: if using absolute positioning or fixed widths, ensure they scale. The flex items-end podium pattern should work at 375px if card widths use percentages/fractions
  - Stats/info sections: stack vertically at base, side-by-side at md:
  - Check for overflow-hidden on containers that might clip content

  ContestPlayView.tsx + ContestReviewView.tsx:
  - Quiz question text: ensure it wraps (break-words if needed)
  - Answer options: full-width buttons at 375px
  - Timer/progress bar: ensure it fits in single line

  ContestCard.tsx:
  - Ensure card content (title, status badge, date) fits at 375px

  LESSON FEATURES:
  LessonsContent.tsx:
  - Session list: ensure cards are full-width
  - Tab/filter bar: if horizontal, ensure scrollable or wrapping

  QuizSection.tsx:
  - Full-screen overlay modes: ensure they work at 375px
  - Question text + answer buttons: full-width
  - Score display: ensure large numbers fit

  ActiveAttendanceCard.tsx + AssignmentHistoryCard.tsx + CurrentAssignmentSection.tsx:
  - File attachment names: use truncate with max-w
  - Date badges (min-w-[36px]): fine, keep as-is
  - Segmented controls: ensure they fit at 375px (the max-w-[280px] pattern in AssignmentHistoryCard is good)

  PROFILE FEATURES:
  ProfileHero.tsx:
  - Avatar + name + stats: stack vertically at 375px
  - Stats row: if using flex with fixed widths, ensure wrapping
  - max-w-[120px] truncation is good, keep

  AchievementShowcase.tsx:
  - Horizontal scroll (overflow-x-auto) for achievements: this is a GOOD pattern at 375px, keep
  - Achievement items at w-[88px]: fine, they scroll horizontally

  ActivityHeatmap.tsx:
  - Grid of small cells: ensure horizontal scroll is enabled if grid is wider than viewport
  - Add overflow-x-auto wrapper if not present

  SHELL:
  Header.tsx:
  - Notification dropdown: w-[280px] at base is fine for 375px (leaves 95px margin)
  - Profile name truncation: max-w-[110px] is good, keep
  - Ensure header actions (bell, theme toggle, avatar) fit at 375px

  Curriculum.tsx + CoinsHistory.tsx:
  - These use overflow patterns -- verify they have overflow-x-auto where needed
  - Ensure content fits or scrolls at 375px

  App.tsx:
  - Bottom nav: verify 5 nav items fit at 375px (375/5 = 75px per item -- tight but doable)
  - If nav labels overflow, consider hiding labels at very small widths or using shorter labels
  - Sidebar (desktop): verify it doesn't affect mobile layout (hidden at base, shown at md:)
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && echo "Fixed-width checks:" && grep -rn "w-\[3[5-9][0-9]px\]\|w-\[[4-9][0-9][0-9]px\]" src/features/ src/components/ src/pages/ 2>/dev/null | grep -v "max-w\|md:\|lg:\|hidden" || echo "OK: no oversized fixed widths"</automated>
  </verify>
  <acceptance_criteria>
  - No fixed-width element wider than 340px at base breakpoint (375px minus 2x padding)
  - Horizontal scrollable sections (achievements, heatmap) use overflow-x-auto
  - All feature component cards are full-width at base
  - Bottom nav 5 items fit at 375px without overflow
  - Contest podium scales down at 375px without clipping
  - Quiz full-screen views work at 375px
  </acceptance_criteria>
  <done>All feature components and shell audited and fixed for 375px responsiveness</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- At 375px viewport width (Chrome DevTools): no horizontal scrollbar on any page
- At 375px: all content readable, no clipped text
- At 375px: all interactive elements tappable
- At 768px viewport width: layouts expand to use more space
</verification>

<success_criteria>
Every page and feature component renders correctly at 375px minimum width. No horizontal overflow, no clipped content, all grids collapse to single column, all text truncates or wraps properly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-02-SUMMARY.md`
</output>
