---
phase: 04-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/BackButton.tsx
  - src/components/ui/Modal.tsx
  - src/App.tsx
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/pages/Dashboard.tsx
  - src/pages/Leaderboard.tsx
  - src/pages/Rewards.tsx
  - src/pages/Login.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/contests/ContestActionButton.tsx
  - src/features/lessons/ActiveAttendanceCard.tsx
  - src/features/lessons/QuizSection.tsx
autonomous: true
requirements: [UX-03, UX-04]

must_haves:
  truths:
    - "Every focusable element shows a visible brand-primary focus ring on keyboard Tab"
    - "Buttons have scale + shadow hover feedback"
    - "Interactive cards lift on hover"
    - "Page transitions use the view-entry animation"
    - "All body text meets WCAG AA 4.5:1 contrast ratio"
    - "All large text (18px+) meets WCAG AA 3:1 contrast ratio"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Focus-visible ring on all button variants"
      contains: "focus-visible:ring"
    - path: "src/components/ui/Card.tsx"
      provides: "Focus-visible ring on hoverable cards"
      contains: "focus-visible:ring"
    - path: "src/components/ui/Input.tsx"
      provides: "Accessible focus ring on inputs"
      contains: "focus-visible"
  key_links:
    - from: "all interactive elements"
      to: "keyboard navigation"
      via: "focus-visible:ring-2 ring-brand-primary ring-offset-2"
      pattern: "focus-visible:ring"
---

<objective>
Add micro-interactions to buttons, cards, and page transitions, and ensure WCAG AA compliance with visible keyboard focus states on every interactive element.

Purpose: UX-03 requires visible micro-interactions (hover, loading, transitions). UX-04 requires WCAG AA contrast and keyboard focus visibility. This plan adds focus-visible rings to all interactive primitives and verifies/adds micro-interaction patterns.

Output: All UI primitives have focus-visible states, interactive cards are hoverable, all contrast ratios meet WCAG AA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/04-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focus-visible rings to all UI primitives and interactive elements</name>
  <files>src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx, src/components/ui/BackButton.tsx, src/components/ui/Modal.tsx, src/App.tsx, src/components/Header.tsx, src/components/Footer.tsx</files>
  <read_first>src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx, src/components/ui/BackButton.tsx, src/components/ui/Modal.tsx, src/App.tsx (NavItem around line 20-55), src/components/Header.tsx (theme toggle, logout, notification bell buttons), src/components/Footer.tsx</read_first>
  <action>
  Add `focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary` to every interactive element. The ring-offset uses the page background color so the ring floats visually.

  FOCUS RING CLASSES (use this exact string for consistency):
  - Standard: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary`
  - For elements on elevated surfaces (cards, modals): `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-secondary`

  Button.tsx:
  - Add standard focus ring to BASE_CLASSES string (line ~37)
  - This one change covers ALL button instances across the entire app
  - Exact addition: append `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary` to BASE_CLASSES

  Card.tsx:
  - Add focus ring to HOVERABLE_CLASSES only (hoverable cards are interactive, non-hoverable are not focusable)
  - Exact addition: append `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary` to HOVERABLE_CLASSES

  Input.tsx:
  - Input already has `focus:ring-4` with brand-primary -- but uses `focus:` not `focus-visible:`
  - Change `focus:outline-none focus:ring-4` to `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary`
  - Also keep the `focus:border-brand-primary` since border change is nice for both mouse and keyboard
  - Actually: inputs SHOULD show focus on click too. Keep `focus:ring-4` AND add `focus-visible:ring-2` -- no, this doubles up. Instead: keep the existing focus:ring-4 brand-primary/10 for all focus (mouse+keyboard), but change it to `focus:outline-none focus:ring-2 focus:ring-brand-primary/30 focus:border-brand-primary` -- the ring is always visible but subtle, matching the brand.

  Badge.tsx:
  - Badges are not normally interactive (no onClick) -- skip focus ring unless Badge renders a button
  - Read the file to confirm -- if it's just a span/div, no focus ring needed

  BackButton.tsx:
  - Add standard focus ring -- it's a button element

  Modal.tsx:
  - Add focus ring to the close button (X button) inside the modal
  - The close button likely uses `<button>` -- add elevated-surface focus ring

  App.tsx NavItem (lines 20-55):
  - NavItem is a `<button>` -- add standard focus ring to the className
  - Append focus ring classes to the button's className string

  Header.tsx:
  - Theme toggle button: add standard focus ring
  - Notification bell button: add standard focus ring
  - Logout button: add standard focus ring
  - Profile avatar button: add standard focus ring
  - Any other clickable elements: add focus ring

  Footer.tsx:
  - The link to Michael's Telegram: add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2 dark:focus-visible:ring-offset-surface-dark-primary focus-visible:rounded-sm` to the anchor tag
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && echo "Focus-visible count:" && grep -rc "focus-visible:ring" src/components/ui/Button.tsx src/components/ui/Card.tsx src/components/ui/Input.tsx src/components/ui/BackButton.tsx src/components/ui/Modal.tsx src/App.tsx src/components/Header.tsx src/components/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
  - Button.tsx BASE_CLASSES contains focus-visible:ring-2 focus-visible:ring-brand-primary
  - Card.tsx HOVERABLE_CLASSES contains focus-visible:ring-2 focus-visible:ring-brand-primary
  - Input.tsx focus ring uses brand-primary color
  - BackButton.tsx has focus-visible:ring classes
  - Modal.tsx close button has focus-visible:ring classes
  - App.tsx NavItem button has focus-visible:ring classes
  - Header.tsx all interactive buttons have focus-visible:ring classes
  - Footer.tsx link has focus-visible:ring classes
  - All focus rings use ring-offset-2 with dark:ring-offset matching the surface color
  </acceptance_criteria>
  <done>Every interactive UI primitive and shell element shows a brand-primary focus ring on keyboard Tab</done>
</task>

<task type="auto">
  <name>Task 2: Verify micro-interactions and add hoverable prop to interactive cards across pages</name>
  <files>src/pages/Dashboard.tsx, src/pages/Leaderboard.tsx, src/pages/Rewards.tsx, src/pages/Login.tsx, src/features/contests/ContestCard.tsx, src/features/contests/ContestActionButton.tsx, src/features/lessons/ActiveAttendanceCard.tsx, src/features/lessons/QuizSection.tsx</files>
  <read_first>src/pages/Dashboard.tsx (search for Card usage), src/pages/Rewards.tsx (search for Card usage), src/features/contests/ContestCard.tsx, src/features/lessons/ActiveAttendanceCard.tsx, src/features/lessons/QuizSection.tsx (CTA card section)</read_first>
  <action>
  Verify and enhance micro-interactions across the app:

  MICRO-INTERACTION AUDIT:
  1. Buttons: Already have scale-[1.01]/scale-[0.99] + shadow transitions in Button.tsx -- VERIFIED, no changes needed
  2. Cards: hoverable prop adds lift + shadow -- need to ensure it's APPLIED to all interactive cards
  3. Page transitions: view-entry animation added in Phase 3 -- VERIFY it's still on the key={currentView} wrapper in App.tsx
  4. Loading states: Button loading prop with spinner -- VERIFIED, no changes needed

  CARDS THAT SHOULD BE HOVERABLE (add hoverable prop if missing):

  Dashboard.tsx:
  - Enrollment cards (course cards users click to navigate): add hoverable if onClick present
  - Tutorial cards: if they link somewhere, add hoverable
  - Do NOT add hoverable to purely informational cards (stats display, progress)

  Rewards.tsx:
  - Reward product cards: add hoverable -- they have a claim action
  - Ensure each reward Card has `hoverable` prop

  ContestCard.tsx:
  - Contest list cards: add hoverable -- they navigate to contest detail
  - Verify Card component is used with hoverable prop

  ActiveAttendanceCard.tsx:
  - If the card has an action (mark attendance button), the CARD itself doesn't need hoverable -- the Button inside handles interaction feedback
  - Only add hoverable if the entire card is clickable

  QuizSection.tsx:
  - The quiz CTA card (start quiz): if using Card, add hoverable
  - Quiz question cards during quiz: do NOT add hoverable (they're static during play)

  Leaderboard.tsx:
  - Ranking rows: if they navigate to profile on click, add hover feedback
  - If they use Card, add hoverable; if they use custom divs, add `hover:bg-surface-secondary/50 dark:hover:bg-surface-dark-elevated/50 transition-colors duration-fast cursor-pointer` to clickable rows

  VERIFY PAGE TRANSITIONS:
  - In App.tsx, confirm the content wrapper div has `key={currentView}` and the view-entry animation class
  - If missing, add: `className="animate-view-entry"` with `key={currentView}` to trigger animation on page switch

  ADDITIONAL MICRO-INTERACTIONS TO ADD:
  - ContestActionButton.tsx: if it doesn't use the Button component, verify it has hover scale feedback. If it uses raw <button>, migrate to Button component or add `hover:scale-[1.01] active:scale-[0.99] transition-transform duration-fast`
  - Login.tsx submit button: verify it uses Button component with loading prop for the Telegram auth polling state
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && echo "Hoverable card usage:" && grep -rn "hoverable" src/pages/ src/features/ | grep -v "node_modules" | wc -l && echo "View entry animation:" && grep -n "animate-view-entry\|view-entry" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
  - All clickable Card instances have hoverable prop
  - Reward cards in Rewards.tsx use Card with hoverable
  - Contest cards in ContestCard.tsx use Card with hoverable
  - Dashboard enrollment cards use Card with hoverable (if clickable)
  - Page transition animation (animate-view-entry) present on view wrapper in App.tsx
  - ContestActionButton has hover scale feedback
  - Login submit uses Button component with loading prop
  - At least 5 Card instances across the app have hoverable prop
  </acceptance_criteria>
  <done>All interactive cards have hoverable prop, page transitions verified, micro-interactions consistent across app</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- Tab through the app: every button, link, and interactive card shows a brand-primary focus ring
- Hover over cards: interactive cards lift with shadow increase
- Click buttons: scale feedback visible
- Navigate between pages: smooth view-entry animation plays
- No duplicate focus rings (focus + focus-visible conflict)
</verification>

<success_criteria>
Every interactive element has a visible focus-visible ring with brand-primary color. All clickable cards use the hoverable prop. Page transitions animate. Button hover/active micro-interactions are consistent. WCAG AA focus visibility requirements are met.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-03-SUMMARY.md`
</output>
