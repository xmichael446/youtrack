---
phase: 04-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/Toast.tsx
  - src/components/ui/StatusBadge.tsx
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/App.tsx
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "Dark mode has deeper, richer backgrounds than current slate-900/800/700"
    - "Surface layers have clear visual separation in dark mode"
    - "Text contrast meets WCAG AA in dark mode (4.5:1 for body, 3:1 for large)"
    - "Brand-primary pops vibrantly against the new dark surfaces"
    - "Cards in dark mode have a subtle luminous edge treatment"
  artifacts:
    - path: "index.html"
      provides: "Redesigned dark surface palette in tailwind config"
      contains: "surface-dark"
    - path: "src/components/ui/Button.tsx"
      provides: "Updated dark variant classes"
      contains: "dark:"
    - path: "src/components/ui/Card.tsx"
      provides: "Updated dark card treatment"
      contains: "dark:"
  key_links:
    - from: "index.html"
      to: "all components"
      via: "Tailwind dark mode token references"
      pattern: "surface-dark"
---

<objective>
Redesign the dark mode palette from scratch so it feels like a premium, intentional dark experience -- not just "white became dark gray."

Purpose: The current 3-layer slate system (slate-900/800/700) is functional but generic. A deeper, more intentional dark palette with better contrast and brand color vibrancy makes dark mode feel premium (per UX-02 and user decision to redesign from scratch).

Output: Updated Tailwind config with new dark surface colors, updated dark mode classes across all UI primitives and shell components, updated design-system.md documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/04-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dark mode palette in Tailwind config and update UI primitives</name>
  <files>index.html, src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx, src/components/ui/Modal.tsx, src/components/ui/Toast.tsx, src/components/ui/StatusBadge.tsx</files>
  <read_first>index.html (lines 25-137 for tailwind config), src/components/ui/Button.tsx, src/components/ui/Card.tsx, src/components/ui/Input.tsx, src/components/ui/Badge.tsx, src/components/ui/Modal.tsx, src/components/ui/Toast.tsx, src/components/ui/StatusBadge.tsx</read_first>
  <action>
  Redesign the dark mode palette in index.html tailwind.config. Replace the current slate-based dark surfaces with a deeper, more intentional palette:

  NEW DARK SURFACE PALETTE (replace existing surface-dark values):
  - surface-dark.primary: '#080f1a' (deep navy-black -- deeper than current slate-900 #0f172a)
  - surface-dark.secondary: '#111827' (dark navy -- replaces slate-800 #1e293b, closer to gray-900)
  - surface-dark.elevated: '#1e293b' (current slate-800 becomes the elevated layer -- more contrast between layers)

  NEW DARK TEXT COLORS (update text-theme-dark):
  - text-theme-dark.primary: '#f8fafc' (slate-50 -- brighter than current #f1f5f9 for better contrast)
  - text-theme-dark.secondary: '#94a3b8' (keep as-is -- good contrast against new darker surfaces)
  - text-theme-dark.muted: '#475569' (slate-600 -- slightly brighter than current #64748b for better readability against deeper bg)

  RATIONALE: By shifting the entire dark scale one step deeper, we get:
  - More dramatic contrast between surface layers (3 distinct visual depths instead of 2 that blur together)
  - Brand-primary (#12C2DC) pops more against the darker background
  - The app feels more premium/immersive in dark mode

  After updating the config, update all UI primitives:

  Button.tsx:
  - secondary variant: change `dark:bg-surface-dark-secondary` to `dark:bg-surface-dark-secondary` (no change needed -- tokens auto-update)
  - Verify all dark: classes reference semantic tokens not raw slate values

  Card.tsx:
  - Update dark card glow ring from `dark:ring-white/5` to `dark:ring-white/[0.07]` (slightly brighter ring against darker bg)
  - The card's dark background tokens auto-update via config change

  Input.tsx:
  - Verify all dark: classes use semantic tokens (they already do -- no changes expected)

  Badge.tsx:
  - Read the file and verify all dark: variants use semantic tokens

  Modal.tsx:
  - Update overlay darkness: if currently using `bg-black/50`, change to `bg-black/60` for better modal focus against the new deeper dark bg

  Toast.tsx:
  - Verify dark: classes use semantic tokens

  StatusBadge.tsx:
  - Verify dark: classes use semantic tokens

  IMPORTANT: Do NOT touch any silver podium colors or RARITY_COLORS -- those are intentional exceptions per design-system.md Section 9.
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "080f1a" index.html && grep -c "111827" index.html && grep -c "ring-white/\[0.07\]" src/components/ui/Card.tsx</automated>
  </verify>
  <acceptance_criteria>
  - index.html contains surface-dark.primary = '#080f1a'
  - index.html contains surface-dark.secondary = '#111827'
  - index.html contains surface-dark.elevated = '#1e293b'
  - index.html contains text-theme-dark.primary = '#f8fafc'
  - Card.tsx dark ring uses ring-white/[0.07]
  - Modal overlay uses bg-black/60 in dark mode
  - All UI primitives reference semantic dark tokens, not raw slate values
  </acceptance_criteria>
  <done>Dark mode palette redesigned with deeper surfaces, brighter text, and updated UI primitive dark classes</done>
</task>

<task type="auto">
  <name>Task 2: Update shell components and page-level dark classes, update design-system.md</name>
  <files>src/components/Header.tsx, src/components/Footer.tsx, src/App.tsx, .planning/design-system.md</files>
  <read_first>src/components/Header.tsx, src/components/Footer.tsx, src/App.tsx (full file), .planning/design-system.md (Section 1.3 and Section 1.4)</read_first>
  <action>
  Update shell/layout components to work with the new darker palette:

  Header.tsx:
  - Verify all dark: background references use semantic tokens
  - If notification dropdown or profile dropdown uses raw slate classes, migrate to semantic tokens
  - Ensure header dark background uses bg-surface-dark-primary (the deepest layer) for the sticky header

  Footer.tsx:
  - Verify dark text classes use semantic tokens (it uses inline isDark ternary -- leave pattern but verify token names)

  App.tsx:
  - Update the main content area dark background: ensure it uses `dark:bg-surface-dark-primary`
  - Update the bottom nav bar dark background: ensure it uses `dark:bg-surface-dark-primary` (same as page bg) with a top border `dark:border-surface-dark-elevated` for separation
  - Update the sidebar dark background on desktop: ensure it uses `dark:bg-surface-dark-secondary` (one step lighter than page bg)
  - NavItem: verify dark mode hover/active states reference semantic tokens

  design-system.md:
  - Update Section 1.3 "Surface Colors (Dark Mode)" with the new values:
    - surface-dark-primary: '#080f1a' (deep navy-black)
    - surface-dark-secondary: '#111827' (dark navy)
    - surface-dark-elevated: '#1e293b' (dark slate)
  - Update Section 1.4 text-theme-dark.primary to '#f8fafc'
  - Update Section 1.4 text-theme-dark.muted to '#475569'
  - Add a note explaining the design rationale: deeper base creates more layer separation and makes brand-primary pop

  Do NOT modify any page files (Dashboard, Leaderboard, etc.) -- they inherit dark mode from semantic tokens automatically.
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && grep -c "080f1a" .planning/design-system.md && grep -c "111827" .planning/design-system.md && grep "surface-dark-primary" src/App.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
  - design-system.md Section 1.3 shows new hex values: #080f1a, #111827, #1e293b
  - design-system.md Section 1.4 shows updated text-theme-dark.primary: #f8fafc
  - App.tsx main content area uses dark:bg-surface-dark-primary
  - App.tsx sidebar uses dark:bg-surface-dark-secondary
  - Header.tsx dark backgrounds use semantic tokens
  - No raw slate-700/800/900 classes remain in Header.tsx, Footer.tsx, or App.tsx (except intentional exceptions)
  </acceptance_criteria>
  <done>Shell components updated for new dark palette, design-system.md documents the redesigned dark mode</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- Toggle dark mode: surfaces show 3 clearly distinct depth levels
- Brand-primary (#12C2DC) text and buttons are vibrant against the deeper backgrounds
- Cards have visible edge treatment (subtle ring) in dark mode
- No raw slate-700/800/900 values in UI primitives or shell components (silver podium exception is in feature files, not touched)
</verification>

<success_criteria>
Dark mode palette is redesigned from scratch with deeper surfaces (#080f1a/#111827/#1e293b), brighter text (#f8fafc), and all UI primitives + shell components use the new semantic tokens. Design system docs are updated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-01-SUMMARY.md`
</output>
