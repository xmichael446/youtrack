---
phase: 01-design-system
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/lessons/ActiveAttendanceCard.tsx
  - src/features/lessons/AssignmentHistoryCard.tsx
  - src/features/lessons/CurrentAssignmentSection.tsx
  - src/features/lessons/LessonsContent.tsx
  - src/features/lessons/QuizSection.tsx
  - src/features/lessons/SubmissionModal.tsx
  - src/features/profile/AchievementShowcase.tsx
  - src/features/profile/ActivityFeed.tsx
  - src/features/profile/ActivityHeatmap.tsx
  - src/features/profile/PrivacySettings.tsx
  - src/features/profile/ProfileEdit.tsx
  - src/features/profile/ProfileHero.tsx
  - src/features/contests/ContestActionButton.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/contests/ContestDetailView.tsx
  - src/features/contests/ContestListView.tsx
  - src/features/contests/ContestPlayView.tsx
  - src/features/contests/ContestSkeletons.tsx
autonomous: true
gap_closure: true
requirements:
  - DS-04
  - DS-02

must_haves:
  truths:
    - "Lesson feature components use semantic radius tokens (rounded-card, rounded-input, rounded-button) instead of raw rounded-xl/2xl/lg"
    - "Profile feature components use semantic radius and duration tokens"
    - "Contest feature components use semantic radius and duration tokens"
    - "Duration tokens (duration-fast, duration-normal) replace raw duration-200/300 across all feature components"
  artifacts:
    - path: "src/features/lessons/QuizSection.tsx"
      provides: "Quiz UI with semantic radius and duration tokens"
    - path: "src/features/contests/ContestDetailView.tsx"
      provides: "Contest detail with semantic tokens (silver podium slate classes preserved)"
    - path: "src/features/profile/ProfileHero.tsx"
      provides: "Profile hero with semantic radius tokens"
    - path: "src/features/contests/ContestCard.tsx"
      provides: "Contest card with semantic radius and duration tokens"
  key_links:
    - from: "src/features/**/*.tsx"
      to: "index.html"
      via: "Tailwind config borderRadius and transitionDuration tokens"
      pattern: "rounded-card|rounded-input|rounded-button|duration-fast|duration-normal"
---

<objective>
Close verification gap 2 for all feature components (lessons, profile, contests) — migrate from raw radius/duration Tailwind classes to semantic design tokens.

Purpose: Complete the semantic token adoption across the remaining 18 feature component files. After this plan, every component in src/ uses the design system's semantic radius and duration tokens.

Output: 18 feature files migrated, achieving full codebase adoption of radius and duration tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/01-design-system/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lessons and profile features to semantic radius/duration tokens</name>
  <files>src/features/lessons/ActiveAttendanceCard.tsx, src/features/lessons/AssignmentHistoryCard.tsx, src/features/lessons/CurrentAssignmentSection.tsx, src/features/lessons/LessonsContent.tsx, src/features/lessons/QuizSection.tsx, src/features/lessons/SubmissionModal.tsx, src/features/profile/AchievementShowcase.tsx, src/features/profile/ActivityFeed.tsx, src/features/profile/ActivityHeatmap.tsx, src/features/profile/PrivacySettings.tsx, src/features/profile/ProfileEdit.tsx, src/features/profile/ProfileHero.tsx</files>
  <read_first>
    - src/features/lessons/ActiveAttendanceCard.tsx
    - src/features/lessons/AssignmentHistoryCard.tsx
    - src/features/lessons/CurrentAssignmentSection.tsx
    - src/features/lessons/LessonsContent.tsx
    - src/features/lessons/QuizSection.tsx
    - src/features/lessons/SubmissionModal.tsx
    - src/features/profile/AchievementShowcase.tsx
    - src/features/profile/ActivityFeed.tsx
    - src/features/profile/ActivityHeatmap.tsx
    - src/features/profile/PrivacySettings.tsx
    - src/features/profile/ProfileEdit.tsx
    - src/features/profile/ProfileHero.tsx
  </read_first>
  <action>
    Apply mechanical radius and duration token replacements across all 12 files.

    **Radius mapping (pixel-equivalent, no visual change):**
    - `rounded-lg` (8px) -> `rounded-button` — buttons, small interactive elements
    - `rounded-xl` (12px) -> `rounded-input` — inputs, small containers, list items
    - `rounded-2xl` (16px) -> `rounded-card` — cards, panels, major containers
    - `rounded-3xl` (24px) -> `rounded-card` — normalizing large containers to card standard

    **IMPORTANT: rounded-full handling:**
    - Do NOT replace `rounded-full` on square elements (avatars like `w-10 h-10 rounded-full`)
    - Only replace `rounded-full` on non-square pill shapes -> `rounded-pill`
    - When in doubt, leave `rounded-full` unchanged

    **Duration mapping:**
    - `duration-200` -> `duration-fast`
    - `duration-300` -> `duration-normal`
    - Leave `duration-500`, `duration-700`, `duration-1000` unchanged

    **Per-file expected changes:**

    Lessons features:
    - `ActiveAttendanceCard.tsx` (~4): rounded-xl/2xl on card containers -> rounded-input/rounded-card
    - `AssignmentHistoryCard.tsx` (~9): rounded-xl/2xl on cards and history items, duration-200/300 on transitions
    - `CurrentAssignmentSection.tsx` (~6): rounded-xl/2xl on section containers, duration-200/300
    - `LessonsContent.tsx` (~2): rounded-xl/2xl on wrapper containers
    - `QuizSection.tsx` (~13): rounded-xl/2xl/lg on quiz cards, option buttons, progress bars, duration-200/300 on hover/click transitions
    - `SubmissionModal.tsx` (~4): rounded-xl/2xl on modal container and inner elements, duration-200/300

    Profile features:
    - `AchievementShowcase.tsx` (~3): rounded-xl/2xl on achievement cards
    - `ActivityFeed.tsx` (~4): rounded-xl/2xl on feed items and containers
    - `ActivityHeatmap.tsx` (~2): rounded-xl on heatmap container
    - `PrivacySettings.tsx` (~4): rounded-xl/2xl on settings panels, duration-200/300 on toggles
    - `ProfileEdit.tsx` (~7): rounded-xl/2xl/lg on form inputs and save button, duration-200/300
    - `ProfileHero.tsx` (~11): rounded-xl/2xl/3xl on hero card and stat blocks, duration-200/300
  </action>
  <verify>
    <automated>count=$(grep -rn 'rounded-xl\|rounded-2xl\|rounded-3xl\|\brounded-lg\b' src/features/lessons/ src/features/profile/ --include='*.tsx' | grep -v 'rounded-\[' | wc -l); dur=$(grep -rn 'duration-200\|duration-300' src/features/lessons/ src/features/profile/ --include='*.tsx' | wc -l); echo "Raw radius: $count, Raw duration: $dur"; test "$count" -eq 0 -a "$dur" -eq 0 &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `rounded-xl`, `rounded-2xl`, `rounded-3xl`, `rounded-lg` across src/features/lessons/ and src/features/profile/ returns 0 matches
    - grep for `duration-200`, `duration-300` across those same directories returns 0 matches
    - grep for `rounded-card\|rounded-input\|rounded-button` returns multiple matches confirming adoption
    - grep for `duration-fast\|duration-normal` returns matches where durations existed
  </acceptance_criteria>
  <done>All 12 lessons and profile feature files use semantic radius and duration tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate contest features to semantic radius/duration tokens</name>
  <files>src/features/contests/ContestActionButton.tsx, src/features/contests/ContestCard.tsx, src/features/contests/ContestDetailView.tsx, src/features/contests/ContestListView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestSkeletons.tsx</files>
  <read_first>
    - src/features/contests/ContestActionButton.tsx
    - src/features/contests/ContestCard.tsx
    - src/features/contests/ContestDetailView.tsx
    - src/features/contests/ContestListView.tsx
    - src/features/contests/ContestPlayView.tsx
    - src/features/contests/ContestSkeletons.tsx
  </read_first>
  <action>
    Apply mechanical radius and duration token replacements across all 6 contest feature files.

    **Same radius and duration mappings as Task 1.**

    **CRITICAL EXCEPTION — ContestDetailView.tsx silver podium:**
    Lines ~229, 234, 239 contain `text-slate-600 dark:text-slate-400` and `bg-slate-300 dark:bg-slate-700` for place===2 silver podium styling. **DO NOT migrate these slate classes** — they are intentional silver theme colors (documented as exception in design-system.md by Plan 05). Only migrate the radius and duration classes in this file.

    **Per-file expected changes:**

    - `ContestActionButton.tsx` (~4): rounded-lg/xl on buttons -> rounded-button/rounded-input, duration-200/300
    - `ContestCard.tsx` (~11): rounded-xl/2xl on card container and inner elements, duration-200/300 on hover transitions
    - `ContestDetailView.tsx` (~19): rounded-xl/2xl/3xl on detail sections, stat cards, buttons. duration-200/300 on transitions. **Preserve silver podium slate classes.** Only replace radius and duration tokens.
    - `ContestListView.tsx` (~6): rounded-xl/2xl on list items and filter tabs, duration-200/300
    - `ContestPlayView.tsx` (~7): rounded-xl/2xl on question cards and answer options, duration-200/300 on selection transitions
    - `ContestSkeletons.tsx` (~6): rounded-xl/2xl on skeleton placeholder shapes
  </action>
  <verify>
    <automated>count=$(grep -rn 'rounded-xl\|rounded-2xl\|rounded-3xl\|\brounded-lg\b' src/features/contests/ --include='*.tsx' | grep -v 'rounded-\[' | wc -l); dur=$(grep -rn 'duration-200\|duration-300' src/features/contests/ --include='*.tsx' | wc -l); echo "Raw radius: $count, Raw duration: $dur"; test "$count" -eq 0 -a "$dur" -eq 0 &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `rounded-xl`, `rounded-2xl`, `rounded-3xl`, `rounded-lg` across src/features/contests/ returns 0 matches
    - grep for `duration-200`, `duration-300` across src/features/contests/ returns 0 matches
    - grep for `rounded-card\|rounded-input\|rounded-button` in contest files returns multiple matches
    - ContestDetailView.tsx still contains `text-slate-600 dark:text-slate-400` and `bg-slate-300 dark:bg-slate-700` for silver podium (intentional exception preserved)
  </acceptance_criteria>
  <done>All 6 contest feature files use semantic radius and duration tokens. Silver podium exception preserved in ContestDetailView.tsx.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify full feature component migration:

```bash
# Zero raw radius tokens in ALL feature directories
grep -rn 'rounded-xl\|rounded-2xl\|rounded-3xl\|\brounded-lg\b' src/features/ --include='*.tsx' | grep -v 'rounded-\[' | wc -l
# Expected: 0

# Zero raw duration tokens in ALL feature directories
grep -rn 'duration-200\|duration-300' src/features/ --include='*.tsx' | wc -l
# Expected: 0

# Semantic tokens are adopted
grep -rn 'rounded-card\|rounded-input\|rounded-button\|duration-fast\|duration-normal' src/features/ --include='*.tsx' | wc -l
# Expected: >40

# Silver exception preserved
grep -n 'text-slate-600\|bg-slate-300' src/features/contests/ContestDetailView.tsx | wc -l
# Expected: >0 (intentional silver styling)

# Full codebase check — combined with Plan 05 results
grep -rn 'rounded-xl\|rounded-2xl\|rounded-3xl\|\brounded-lg\b' src/ --include='*.tsx' --include='*.ts' | grep -v 'rounded-\[' | wc -l
# Expected: 0 (entire src/ clean)

grep -rn 'duration-200\|duration-300' src/ --include='*.tsx' --include='*.ts' | wc -l
# Expected: 0 (entire src/ clean)
```
</verification>

<success_criteria>
- All 18 feature files use semantic radius tokens — zero raw rounded-xl/2xl/3xl/lg in src/features/
- All 18 feature files use semantic duration tokens — zero raw duration-200/300 in src/features/
- Silver podium slate classes in ContestDetailView.tsx preserved (intentional exception)
- Combined with Plan 05: entire src/ directory has zero raw radius and duration tokens
- Dev server still compiles without errors (npm run dev)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-06-SUMMARY.md`
</output>
