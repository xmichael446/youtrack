---
phase: 01-design-system
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/features/contests/ContestListView.tsx
  - src/features/contests/ContestDetailView.tsx
  - src/features/contests/ContestPlayView.tsx
  - src/features/contests/ContestReviewView.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/contests/ContestActionButton.tsx
  - src/features/contests/ContestSkeletons.tsx
  - src/features/lessons/lessonTypes.ts
  - src/utils/contestHelpers.ts
  - src/pages/Contests.tsx
  - src/components/ui/Toast.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04

must_haves:
  truths:
    - "The entire app renders correctly in both light and dark themes with zero raw slate/gray Tailwind dark mode classes"
    - "All text across the app uses the typography scale tokens — no raw text-sm/text-xl/font-bold combos remain outside of intentional exceptions"
    - "No off-scale spacing values remain anywhere in src/"
    - "Contest features display correctly with consistent visual language matching the rest of the app"
  artifacts:
    - path: "src/features/contests/ContestListView.tsx"
      provides: "Contest list with semantic tokens"
    - path: "src/features/contests/ContestCard.tsx"
      provides: "Contest card with semantic tokens"
    - path: "src/pages/Contests.tsx"
      provides: "Contest page wrapper with semantic tokens"
  key_links:
    - from: "src/features/contests/ContestCard.tsx"
      to: "index.html"
      via: "Tailwind classes reference tokens defined in config"
      pattern: "surface-dark-|shadow-card|text-h[1-4]|text-body"
---

<objective>
Migrate contest features and remaining files to semantic design tokens, then verify full codebase completion across colors, typography, and spacing.

Purpose: Eliminate ALL remaining raw slate/gray color classes, raw typography utilities, and off-scale spacing from the codebase so the design system is consistently applied everywhere.
Output: 11 files updated. Full codebase migration complete across all three token dimensions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/design-system.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contest feature components (colors, typography, spacing)</name>
  <files>src/features/contests/ContestListView.tsx, src/features/contests/ContestDetailView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestReviewView.tsx, src/features/contests/ContestCard.tsx, src/features/contests/ContestActionButton.tsx, src/features/contests/ContestSkeletons.tsx</files>
  <read_first>
    - .planning/design-system.md (token reference, typography migration table, spacing approach)
    - src/features/contests/ContestListView.tsx
    - src/features/contests/ContestDetailView.tsx (~42 typography occurrences — heaviest contest file)
    - src/features/contests/ContestCard.tsx
    - src/features/contests/ContestPlayView.tsx (~20 typography)
    - src/features/contests/ContestReviewView.tsx (~9 typography)
    - src/features/contests/ContestActionButton.tsx
    - src/features/contests/ContestSkeletons.tsx
  </read_first>
  <action>
Apply THREE categories of replacement to all 7 contest feature files:

**A. Color replacements:**
- `dark:bg-slate-900` -> `dark:bg-surface-dark-primary`
- `dark:bg-slate-800` -> `dark:bg-surface-dark-secondary`
- `dark:bg-slate-700` -> `dark:bg-surface-dark-elevated`
- `dark:bg-gray-800` -> `dark:bg-surface-dark-secondary`
- `bg-white` (card/container bg) -> `bg-surface-primary`
- `bg-gray-50` or `bg-gray-100` -> `bg-surface-secondary`
- Text: same slate->semantic mappings as previous plans
- Border: same mappings
- Shadow: ContestCard.tsx cards -> `shadow-card dark:shadow-card-dark`
- ContestSkeletons.tsx: Replace shimmer background colors with semantic surface tokens, keep shimmer animation class

Also check for isDark ternary patterns in contest files — if found, migrate using semantic tokens.

**B. Typography migration:**

ContestDetailView.tsx (~42 — heaviest):
- Contest title -> text-h2
- Section headings (Rules, Prizes, etc.) -> text-h3
- Detail labels -> text-label or text-h4
- Body descriptions -> text-body
- Metadata (dates, participant count) -> text-caption
- Button text -> text-body

ContestPlayView.tsx (~20):
- Question text -> text-h3
- Timer display -> text-h1 or text-h2
- Score/progress -> text-h3
- Answer options -> text-body
- Hints -> text-caption

ContestReviewView.tsx (~9):
- Review heading -> text-h2
- Question review -> text-body
- Correct/incorrect labels -> text-label
- Score summary -> text-h3

ContestCard.tsx (~8):
- Card title -> text-h4
- Status/date -> text-caption
- Prize info -> text-body

ContestListView.tsx (~6): Page heading -> text-h2, filters -> text-body
ContestActionButton.tsx (~4): Button text -> text-body or text-label
ContestSkeletons.tsx: Minimal typography, just placeholder sizing

Remove separate font-weight classes when semantic token already includes fontWeight.

**C. Off-scale spacing:** Audit and replace off-scale values (p-2.5, p-5, p-7, etc.) with nearest on-scale equivalents.

Rules: No functional changes. Preserve contest logic (registration, timer, quiz flow). Keep brand-primary accents.
  </action>
  <verify>
    <automated>echo "=== Color check ===" && grep -rn "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/features/contests/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS: no raw slate in contests" : "FAIL: "$1" raw slate references"}' && echo "=== Off-scale spacing ===" && grep -roP '(?:p|px|py|m|mx|my|mt|mb|ml|mr|gap)-(?:2\.5|3\.5|5|7|9|10|11|14|1\.5)\b' src/features/contests/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS" : "WARN: "$1" off-scale values"}'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 contest feature files: zero raw dark:*-slate-* classes
    - All 7 contest feature files: typography uses semantic scale tokens
    - ContestCard.tsx uses "shadow-card" token
    - ContestSkeletons.tsx still has shimmer animation
    - No off-scale spacing values remain
    - All contest component exports and props unchanged
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 7 contest feature files migrated to semantic tokens for colors, typography, and spacing. No raw slate dark mode classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining files and verify full codebase completion</name>
  <files>src/features/lessons/lessonTypes.ts, src/utils/contestHelpers.ts, src/pages/Contests.tsx, src/components/ui/Toast.tsx</files>
  <read_first>
    - .planning/design-system.md (token reference)
    - src/features/lessons/lessonTypes.ts
    - src/utils/contestHelpers.ts
    - src/pages/Contests.tsx
    - src/components/ui/Toast.tsx
  </read_first>
  <action>
**Migrate remaining 4 files:**

**src/pages/Contests.tsx:**
- Apply standard dark mode replacements (slate -> semantic surface/text tokens)
- Typography: Replace raw text-sm/text-xl + font-weight with semantic scale tokens
- Spacing: Fix off-scale values

**src/components/ui/Toast.tsx:**
- Apply color and typography replacements
- This file was not in previous plans' files_modified lists — ensure it gets migrated

**src/features/lessons/lessonTypes.ts:**
- Check for any Tailwind class strings in type definitions or constants
- Replace raw slate/gray references with semantic token class names
- If no Tailwind classes present (pure type file), no changes needed

**src/utils/contestHelpers.ts:**
- Check for any Tailwind class strings in utility functions (e.g., status color mappings)
- Replace raw slate/gray/typography references with semantic token equivalents
- If no Tailwind classes present (pure logic), no changes needed

**Full codebase verification (ALL THREE dimensions):**

After migrating these files, run comprehensive checks across the entire `src/` directory:

1. **Colors:** `grep -rn "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/`
   - Also check: `grep -rn "isDark.*slate\|isDark.*gray" src/` for ternary patterns
   - Goal: ZERO raw dark:*-slate-* Tailwind classes AND zero isDark ternaries with slate/gray

2. **Typography:** Spot-check that key pages use semantic tokens. Not every `text-sm` needs to be replaced (some may be in contexts where none of the semantic tokens fit), but major headings, body text, and captions should use the scale.

3. **Spacing:** `grep -roP '(?:p|px|py|m|mx|my|mt|mb|ml|mr|gap)-(?:2\.5|3\.5|5|7|9|10|11|14|1\.5)\b' src/` for off-scale values. A few `p-0.5` for micro-alignment are acceptable.

**Note on NotificationContext.tsx:** This file (`src/context/NotificationContext.tsx`) is a pure context/logic file with zero className attributes — it requires no migration. If the codebase check finds other notification-related files with styling, fix those too.

If ANY raw slate dark mode classes or isDark ternaries with slate/gray are found in files not covered by Plans 02-04, fix them in this task.
  </action>
  <verify>
    <automated>echo "=== Full codebase color check ===" && grep -rn "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS: zero raw slate classes in entire src/" : "FAIL: "$1" raw slate references remain"}' && echo "=== isDark ternary check ===" && grep -rn "isDark.*slate\|isDark.*gray" src/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS: zero isDark slate/gray ternaries" : "FAIL: "$1" isDark ternaries remain"}' && echo "=== Off-scale spacing check ===" && grep -roP '(?:p|px|py|m|mx|my|mt|mb|ml|mr|gap)-(?:2\.5|3\.5|5|7|9|10|11|14|1\.5)\b' src/ 2>/dev/null | grep -v "0\.5" | wc -l | awk '{print ($1 == 0) ? "PASS: no off-scale spacing (excluding 0.5 micro-adjustments)" : "WARN: "$1" off-scale spacing values remain"}' && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/` returns zero matches
    - `grep -r "isDark.*slate\|isDark.*gray" src/` returns zero matches
    - Off-scale spacing values eliminated (p-0.5 micro-adjustments acceptable)
    - src/pages/Contests.tsx contains semantic surface/text/typography tokens
    - `npm run build` succeeds with no errors
    - All TypeScript type definitions unchanged (only class string constants modified if any)
  </acceptance_criteria>
  <done>Full codebase migration complete. Zero raw dark:*-slate-* Tailwind classes in src/. Zero isDark ternaries with slate/gray. Typography scale applied. Off-scale spacing corrected. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -r "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/` returns ZERO matches
2. `grep -r "isDark.*slate\|isDark.*gray" src/` returns ZERO matches
3. `grep -roP '(?:p|px|py|m|mx|my|mt|mb|ml|mr|gap)-(?:2\.5|3\.5|5|7|9|10|11|14|1\.5)\b' src/ | grep -v "0\.5"` returns ZERO matches
4. `npm run build` succeeds
5. All contest functionality preserved (registration, timer, play, review flows)
6. Design system tokens are the sole source of theming across entire codebase
</verification>

<success_criteria>
- Complete codebase migration: zero raw dark:*-slate-* classes in src/
- Complete isDark ternary migration: zero ternaries with raw slate/gray
- Typography scale tokens applied across all feature files
- Off-scale spacing corrected throughout
- Contest features fully migrated
- Build passes
- This plan, combined with Plans 02 and 03, means all component files use the design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-04-SUMMARY.md`
</output>
