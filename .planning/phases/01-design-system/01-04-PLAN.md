---
phase: 01-design-system
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/features/contests/ContestListView.tsx
  - src/features/contests/ContestDetailView.tsx
  - src/features/contests/ContestPlayView.tsx
  - src/features/contests/ContestReviewView.tsx
  - src/features/contests/ContestCard.tsx
  - src/features/contests/ContestActionButton.tsx
  - src/features/contests/ContestSkeletons.tsx
  - src/features/lessons/lessonTypes.ts
  - src/utils/contestHelpers.ts
  - src/pages/Contests.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-04

must_haves:
  truths:
    - "Contest feature components use semantic tokens instead of raw slate colors"
    - "Contest utility and type files use semantic tokens where applicable"
    - "The entire src/ directory has no remaining raw dark:bg-slate-* or dark:text-slate-* Tailwind classes"
  artifacts:
    - path: "src/features/contests/ContestListView.tsx"
      provides: "Contest list with semantic tokens"
    - path: "src/features/contests/ContestCard.tsx"
      provides: "Contest card with semantic tokens"
    - path: "src/pages/Contests.tsx"
      provides: "Contest page wrapper with semantic tokens"
  key_links:
    - from: "src/features/contests/ContestCard.tsx"
      to: "index.html"
      via: "Tailwind classes reference tokens defined in config"
      pattern: "surface-dark-|shadow-card"
---

<objective>
Migrate contest features and remaining utility files to semantic design tokens, completing the full codebase migration.

Purpose: Eliminate all remaining raw slate/gray color classes from the codebase so the design system is consistently applied everywhere.
Output: 10 files updated. Full codebase migration complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/design-system.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contest feature components to semantic tokens</name>
  <files>src/features/contests/ContestListView.tsx, src/features/contests/ContestDetailView.tsx, src/features/contests/ContestPlayView.tsx, src/features/contests/ContestReviewView.tsx, src/features/contests/ContestCard.tsx, src/features/contests/ContestActionButton.tsx, src/features/contests/ContestSkeletons.tsx</files>
  <read_first>
    - .planning/design-system.md (token reference)
    - src/features/contests/ContestListView.tsx
    - src/features/contests/ContestDetailView.tsx
    - src/features/contests/ContestCard.tsx
    - src/features/contests/ContestPlayView.tsx
    - src/features/contests/ContestReviewView.tsx
    - src/features/contests/ContestActionButton.tsx
    - src/features/contests/ContestSkeletons.tsx
  </read_first>
  <action>
Apply the standard replacement pattern to all 7 contest feature files:

**Background replacements:**
- `dark:bg-slate-900` -> `dark:bg-surface-dark-primary`
- `dark:bg-slate-800` -> `dark:bg-surface-dark-secondary`
- `dark:bg-slate-700` -> `dark:bg-surface-dark-elevated`
- `dark:bg-gray-800` -> `dark:bg-surface-dark-secondary`
- `bg-white` (card/container bg) -> `bg-surface-primary`
- `bg-gray-50` or `bg-gray-100` -> `bg-surface-secondary`

**Text replacements:**
- `dark:text-white` -> `dark:text-text-theme-dark-primary`
- `dark:text-slate-100` through `dark:text-slate-200` -> `dark:text-text-theme-dark-primary`
- `dark:text-slate-300` or `dark:text-slate-400` -> `dark:text-text-theme-dark-secondary`
- `dark:text-slate-500` -> `dark:text-text-theme-dark-muted`
- `text-gray-900` -> `text-text-theme-primary`
- `text-gray-600` or `text-gray-500` -> `text-text-theme-secondary`
- `text-gray-400` -> `text-text-theme-muted`

**Border replacements:**
- `dark:border-slate-700` or `dark:border-slate-600` -> `dark:border-surface-dark-elevated`
- `border-gray-200` or `border-gray-100` -> `border-surface-secondary`

**Shadow replacements:**
- Card-level shadow on ContestCard.tsx: use `shadow-card dark:shadow-card-dark`
- Modal/overlay shadows in detail views: use `shadow-modal dark:shadow-modal-dark`

**ContestSkeletons.tsx:**
- Replace shimmer background colors with semantic surface tokens
- Keep the shimmer animation class (uses existing keyframe)

Rules: No functional changes. Preserve contest logic (registration, timer, quiz flow). Keep brand-primary accents.
  </action>
  <verify>
    <automated>grep -rn "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/features/contests/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS" : "FAIL: "$1" raw slate references remain in contests"}'</automated>
  </verify>
  <acceptance_criteria>
    - All 7 contest feature files contain zero "dark:bg-slate-", "dark:text-slate-", or "dark:border-slate-" classes
    - ContestCard.tsx uses "shadow-card" token
    - ContestSkeletons.tsx still has shimmer animation
    - All contest component exports and props unchanged
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All 7 contest feature files migrated to semantic tokens. No raw slate dark mode classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining files and verify full codebase completion</name>
  <files>src/features/lessons/lessonTypes.ts, src/utils/contestHelpers.ts, src/pages/Contests.tsx</files>
  <read_first>
    - .planning/design-system.md (token reference)
    - src/features/lessons/lessonTypes.ts
    - src/utils/contestHelpers.ts
    - src/pages/Contests.tsx
  </read_first>
  <action>
**Migrate remaining 3 files:**

**src/pages/Contests.tsx:**
- Apply standard dark mode replacements (slate -> semantic surface/text tokens)
- This is the page wrapper that delegates to contest feature views

**src/features/lessons/lessonTypes.ts:**
- Check for any Tailwind class strings in type definitions or constants
- Replace raw slate/gray references with semantic token class names
- If no Tailwind classes present (pure type file), no changes needed

**src/utils/contestHelpers.ts:**
- Check for any Tailwind class strings in utility functions
- Replace raw slate/gray references with semantic token class names
- If no Tailwind classes present (pure logic), no changes needed

**Full codebase verification:**
After migrating these files, run a comprehensive grep across the entire `src/` directory to find any remaining `dark:bg-slate-` or `dark:text-slate-` or `dark:border-slate-` classes. If any are found in files not covered by Plans 02-04, fix them in this task.

The goal: ZERO raw dark:*-slate-* Tailwind classes anywhere in `src/`.
  </action>
  <verify>
    <automated>echo "=== Full codebase check ===" && grep -rn "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/ 2>/dev/null | wc -l | awk '{print ($1 == 0) ? "PASS: zero raw slate classes in entire src/" : "FAIL: "$1" raw slate references remain"}' && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/` returns zero matches
    - src/pages/Contests.tsx contains semantic surface/text tokens
    - `npm run build` succeeds with no errors
    - All TypeScript type definitions unchanged (only class string constants modified if any)
  </acceptance_criteria>
  <done>Full codebase migration complete. Zero raw dark:*-slate-* Tailwind classes in src/. All files use semantic design tokens. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -r "dark:bg-slate-\|dark:text-slate-\|dark:border-slate-" src/` returns ZERO matches
2. `npm run build` succeeds
3. All contest functionality preserved (registration, timer, play, review flows)
4. Design system tokens are the sole source of dark mode surface/text colors across entire codebase
</verification>

<success_criteria>
- Complete codebase migration: zero raw dark:*-slate-* classes in src/
- Contest features fully migrated
- Build passes
- This plan, combined with Plans 02 and 03, means all component files use the design system
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-04-SUMMARY.md`
</output>
