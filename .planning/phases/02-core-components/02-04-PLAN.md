---
phase: 02-core-components
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/App.tsx
  - src/components/Header.tsx
  - src/components/ui/index.ts
autonomous: true
requirements: [COMP-07]

must_haves:
  truths:
    - "Mobile bottom nav active state uses brand-primary underline/fill + semibold text"
    - "Desktop sidebar nav active state uses brand-primary indicator + semibold text"
    - "Header dropdowns use z-index from shared scale (Z_INDEX.dropdown = 40)"
    - "All UI primitives are importable from a single barrel file"
    - "Navigation components feel like one unified system"
  artifacts:
    - path: "src/App.tsx"
      provides: "Updated NavItem with unified active state treatment"
    - path: "src/components/Header.tsx"
      provides: "Header with z-index scale for dropdowns"
    - path: "src/components/ui/index.ts"
      provides: "Barrel export for all UI primitives"
      exports: ["Button", "Card", "Input", "Badge", "EmptyState", "Modal", "Toast"]
  key_links:
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/*.tsx"
      via: "re-export"
      pattern: "export.*from"
    - from: "src/App.tsx"
      to: "design-system tokens"
      via: "NavItem active state"
      pattern: "brand-primary.*underline\|border-b.*brand-primary"
    - from: "src/components/Header.tsx"
      to: "src/constants/zIndex.ts"
      via: "import Z_INDEX"
      pattern: "Z_INDEX"
---

<objective>
Standardize navigation active states, align z-index usage in Header, and create the barrel export file for all UI primitives.

Purpose: COMP-07 requires unified navigation treatment. The bottom nav, sidebar, and header currently have different active state visuals. This plan unifies them to use brand-primary underline/fill + semibold text. The barrel file makes all new components easy to import from one path.

Output: Updated App.tsx (NavItem), updated Header.tsx (z-index), new barrel file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/design-system.md
@.planning/phases/02-core-components/02-CONTEXT.md
@.planning/phases/02-core-components/02-01-SUMMARY.md
@.planning/phases/02-core-components/02-02-SUMMARY.md
@.planning/phases/02-core-components/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize NavItem active state and create barrel file</name>
  <files>src/App.tsx, src/components/ui/index.ts</files>
  <read_first>
    - src/App.tsx (full file -- NavItem component definition around line 20, bottom nav around line 274, sidebar around line 231)
    - src/components/ui/Button.tsx (verify it exists from Plan 01)
    - src/components/ui/Card.tsx (verify it exists from Plan 01)
    - src/components/ui/Input.tsx (verify it exists from Plan 02)
    - src/components/ui/Badge.tsx (verify it exists from Plan 02)
    - src/components/ui/EmptyState.tsx (verify it exists from Plan 02)
    - src/components/ui/Modal.tsx (verify it exists from Plan 03)
    - src/components/ui/Toast.tsx (verify it exists from Plan 03)
    - .planning/phases/02-core-components/02-CONTEXT.md (Nav decisions: brand-primary underline/fill, semibold text)
  </read_first>
  <action>
    **Step 1: Create barrel file `src/components/ui/index.ts`**

    ```typescript
    export { default as Button } from './Button';
    export { default as Card } from './Card';
    export { default as Input } from './Input';
    export { default as Badge } from './Badge';
    export { default as EmptyState } from './EmptyState';
    export { default as Modal } from './Modal';
    export { default as Toast } from './Toast';
    export { default as Shimmer } from './Shimmer';
    export { default as BackButton } from './BackButton';
    export { default as StatusBadge } from './StatusBadge';
    export { default as RankBadge } from './RankBadge';
    export { default as PlaceIcon } from './PlaceIcon';
    export { default as LeaderboardAvatar } from './LeaderboardAvatar';
    ```

    **Step 2: Update NavItem active state in `src/App.tsx`**

    Find the `NavItem` component (around line 20). Update the active state styling to use a unified treatment:

    Current mobile active state: `text-brand-primary bg-brand-primary/8 dark:bg-brand-primary/12 ...`

    Updated mobile active state (add brand-primary bottom indicator):
    - Active: `text-brand-primary font-semibold` + render a bottom indicator bar: a `<div>` after the label with classes `absolute bottom-0 left-1/2 -translate-x-1/2 w-5 h-[3px] bg-brand-primary rounded-full shadow-[0_0_6px_rgba(18,194,220,0.5)]`
    - Keep the existing bg-brand-primary/8 tint for active
    - Keep the current icon scale-110 and stroke-[2.5px] for active

    For the **desktop sidebar** active state (also in the NavItem since it's shared):
    - The existing left indicator bar (`w-1 h-6 bg-brand-primary rounded-r-full`) is already good
    - Add `font-semibold` to the label text when active
    - Keep the existing `bg-gradient-to-r from-brand-primary/10 to-transparent`

    Inactive state: keep existing `text-text-theme-muted dark:text-text-theme-dark-muted hover:text-brand-primary`. Ensure text is NOT semibold when inactive (use `font-normal` or omit font-weight).

    The NavItem label `<span>` around line 45-47 -- add font-weight control:
    - Active: add `font-semibold` to the label span
    - Inactive: ensure no bold/semibold on label

    Add `relative` to the NavItem button if not already present (needed for absolute-positioned bottom indicator).
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && npx tsc --noEmit src/components/ui/index.ts src/App.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export.*Button.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "export.*Card.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "export.*Modal.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "export.*Input.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "export.*Badge.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "export.*EmptyState.*from" src/components/ui/index.ts` returns at least 1
    - `grep -c "font-semibold" src/App.tsx` returns at least 1 (active nav label)
    - `grep -c "bg-brand-primary rounded-full" src/App.tsx` returns at least 1 (bottom indicator)
  </acceptance_criteria>
  <done>Barrel file exports all 13 UI components. NavItem active state uses brand-primary bottom indicator bar on mobile and font-semibold label text on both mobile and desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Align Header dropdown z-index with shared scale</name>
  <files>src/components/Header.tsx</files>
  <read_first>
    - src/components/Header.tsx (full file -- notification panel z-50 at line 231, profile panel z-50 at line 308)
    - src/constants/zIndex.ts (z-index scale constants)
  </read_first>
  <action>
    Update `src/components/Header.tsx`:

    1. **Import z-index constants**: Add `import { Z_INDEX } from '../constants/zIndex';` at the top.

    2. **Update header z-index**: The `<header>` element currently uses `z-30`. Update to use inline style `style={{ zIndex: Z_INDEX.dropdown }}` which gives z-index: 40. This is appropriate since the header sits below overlays (50) and modals (60).

    3. **Update notification dropdown z-index**: The notification panel (around line 231) currently uses `z-50`. Change to use inline style `style={{ zIndex: Z_INDEX.overlay }}` (50) -- this keeps the current value but references the scale.

    4. **Update profile dropdown z-index**: The profile panel (around line 308) currently uses `z-50`. Change to use inline style `style={{ zIndex: Z_INDEX.overlay }}` (50).

    5. Remove the Tailwind z-index classes (`z-30`, `z-50`) from those elements since we're using inline styles instead. Keep the rest of the className strings intact.

    6. **Do not change any other behavior** -- the dropdown open/close logic, ESC handler, click-outside handler, and all other functionality remain exactly as-is.
  </action>
  <verify>
    <automated>cd /home/xmichael446/WebstormProjects/YouTrack && npx tsc --noEmit src/components/Header.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Z_INDEX" src/components/Header.tsx` returns at least 3 (import + 2-3 usages)
    - `grep -c "import.*zIndex" src/components/Header.tsx` returns 1
    - `grep -c "z-30" src/components/Header.tsx` returns 0 (replaced with Z_INDEX)
    - Notification and profile dropdowns no longer use hardcoded `z-50` class (use Z_INDEX.overlay inline style instead)
  </acceptance_criteria>
  <done>Header uses Z_INDEX constants for z-index layering. Header element at Z_INDEX.dropdown (40), dropdowns at Z_INDEX.overlay (50). No more hardcoded z-index Tailwind classes in Header.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for App.tsx, Header.tsx, and index.ts
- Barrel file correctly exports all components (Button, Card, Input, Badge, EmptyState, Modal, Toast, Shimmer, BackButton, StatusBadge, RankBadge, PlaceIcon, LeaderboardAvatar)
- NavItem active state has brand-primary indicator bar and semibold text
- Header z-index values come from Z_INDEX constants
- `npm run dev` starts without errors
</verification>

<success_criteria>
- All UI primitives importable from `src/components/ui/index.ts`
- Navigation active states are unified: brand-primary indicator + semibold text
- Header dropdowns use shared z-index scale
- No z-index conflicts between nav (40), dropdowns (50), modals (60), and toasts (70)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-components/02-04-SUMMARY.md`
</output>
