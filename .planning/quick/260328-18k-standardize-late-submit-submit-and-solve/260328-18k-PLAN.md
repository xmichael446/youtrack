---
phase: quick
plan: 260328-18k
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/lessons/CurrentAssignmentSection.tsx
  - src/features/lessons/AssignmentHistoryCard.tsx
  - src/features/lessons/QuizSection.tsx
  - src/features/lessons/SubmissionModal.tsx
autonomous: true
must_haves:
  truths:
    - "Submit/late-submit buttons are content-width, not stretched to full container"
    - "Finish Quiz button uses the Button component, not a raw <button>"
    - "Start Quiz button is content-width, not flex-stretched"
    - "All action buttons share consistent sizing (md) and use the Button component"
  artifacts:
    - path: "src/features/lessons/CurrentAssignmentSection.tsx"
      provides: "Submit button without fullWidth"
    - path: "src/features/lessons/AssignmentHistoryCard.tsx"
      provides: "Submit button without fullWidth, size md"
    - path: "src/features/lessons/QuizSection.tsx"
      provides: "Finish Quiz using Button component, Start Quiz without flex-1"
    - path: "src/features/lessons/SubmissionModal.tsx"
      provides: "Modal footer buttons without flex-1"
  key_links: []
---

<objective>
Standardize all submit/quiz action buttons to use the Button component consistently, remove fullWidth and flex-1 stretching, and ensure uniform sizing.

Purpose: Visual consistency across all lesson action buttons — they should be content-sized, not stretched.
Output: Four files updated with consistent Button usage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/ui/Button.tsx
@src/features/lessons/CurrentAssignmentSection.tsx
@src/features/lessons/AssignmentHistoryCard.tsx
@src/features/lessons/QuizSection.tsx
@src/features/lessons/SubmissionModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fullWidth from submit buttons and standardize size</name>
  <files>src/features/lessons/CurrentAssignmentSection.tsx, src/features/lessons/AssignmentHistoryCard.tsx, src/features/lessons/SubmissionModal.tsx</files>
  <action>
In CurrentAssignmentSection.tsx (~line 113): Remove the `fullWidth` prop from the submit Button. Keep everything else (variant, size="md", icon, orange override className for overdue).

In AssignmentHistoryCard.tsx (~line 124-125): Remove the `fullWidth` prop AND change `size="sm"` to `size="md"` for consistency with the other submit buttons. Keep orange override className for overdue.

In SubmissionModal.tsx (~lines 82-99): Remove `className="flex-1"` from BOTH the Cancel (ghost) button and the Submit (primary) button. The modal footer flex container will auto-size both buttons to their content width. Add `className="min-w-[100px]"` to both buttons so they don't collapse too small in the modal footer.
  </action>
  <verify>Run `npm run build` to confirm no TypeScript or build errors. Grep for `fullWidth` in the lessons feature directory — should return zero matches. Grep for `flex-1` in SubmissionModal.tsx — should return zero matches.</verify>
  <done>Submit buttons are content-width in both CurrentAssignmentSection, AssignmentHistoryCard, and SubmissionModal. AssignmentHistoryCard button uses size="md".</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw Finish Quiz button with Button component and remove Start Quiz flex-1</name>
  <files>src/features/lessons/QuizSection.tsx</files>
  <action>
At ~line 287-296, replace the raw `<button>` element for "Finish Quiz" with the Button component:
- Use `variant="primary"` and `size="lg"` (this is a prominent final action)
- Use `loading={submitting}` prop instead of the manual ternary for the spinner
- Use `icon={<Trophy className="w-4 h-4" />}` for the trophy icon
- Keep `disabled={submitting || Object.keys(answers).length < questionsData!.questions.length}`
- Keep `onClick={handleSubmitQuiz}`
- Add `className="uppercase tracking-widest"` to preserve the existing uppercase styling
- Remove all inline Tailwind classes that the Button component already handles (bg, text color, rounded, font-bold, shadow, transition, active:scale, flex items-center justify-center gap)

At ~line 477, remove `className="flex-1"` from the Start Quiz Button so it is content-width instead of stretching in the flex container.

Ensure Button is imported at the top of the file. Check if it is already imported; if not, add: `import Button from '@/components/ui/Button';` (or from the barrel `'@/components/ui'` if that pattern is used in this file).
  </action>
  <verify>Run `npm run build` to confirm no TypeScript or build errors. Grep QuizSection.tsx for raw `<button` tags — the Finish Quiz raw button should be gone (note: the Previous button and review-mode buttons are out of scope and will remain as raw buttons). Grep for `flex-1` in the Start Quiz area — should be gone.</verify>
  <done>Finish Quiz uses the Button component with variant="primary" size="lg" and loading prop. Start Quiz button is content-width. Build passes.</done>
</task>

</tasks>

<verification>
```bash
npm run build
```
Build completes with no errors.

Visual check: All submit/quiz buttons should appear as content-width (not full-width stretched) and the Finish Quiz button should match the design system's primary button styling.
</verification>

<success_criteria>
- No `fullWidth` prop on any submit button in the lessons feature
- No `flex-1` on Start Quiz or SubmissionModal buttons
- Finish Quiz uses Button component (not raw `<button>`)
- AssignmentHistoryCard submit button uses size="md"
- `npm run build` passes cleanly
</success_criteria>
