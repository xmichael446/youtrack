---
phase: quick
plan: 260328-18k
subsystem: lessons
tags: [buttons, consistency, design-system]
dependency_graph:
  requires: []
  provides: [consistent-lesson-action-buttons]
  affects: [CurrentAssignmentSection, AssignmentHistoryCard, QuizSection, SubmissionModal]
tech_stack:
  added: []
  patterns: [Button component with loading prop, content-width buttons]
key_files:
  created: []
  modified:
    - src/features/lessons/CurrentAssignmentSection.tsx
    - src/features/lessons/AssignmentHistoryCard.tsx
    - src/features/lessons/SubmissionModal.tsx
    - src/features/lessons/QuizSection.tsx
decisions:
  - Finish Quiz uses size=lg (prominent final action) vs size=md for submit buttons
  - SubmissionModal footer uses min-w-[100px] instead of flex-1 to prevent collapse in modal
metrics:
  duration: ~5m
  completed: 2026-03-28
  tasks_completed: 2
  files_modified: 4
---

# Quick Task 260328-18k: Standardize Late-Submit, Submit and Solve Buttons Summary

**One-liner:** Removed fullWidth/flex-1 stretching from all lesson action buttons and migrated Finish Quiz from raw button to design system Button component.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Remove fullWidth and flex-1 from submit buttons | be2c4ec | CurrentAssignmentSection.tsx, AssignmentHistoryCard.tsx, SubmissionModal.tsx |
| 2 | Replace raw Finish Quiz button, remove Start Quiz flex-1 | 708f8fe | QuizSection.tsx |

## Changes Made

**CurrentAssignmentSection.tsx:** Removed `fullWidth` prop from the submit/late-submit Button. Button is now content-width.

**AssignmentHistoryCard.tsx:** Removed `fullWidth` prop and changed `size="sm"` to `size="md"` to match other submit buttons. Button is now content-width with consistent size.

**SubmissionModal.tsx:** Replaced `className="flex-1"` with `className="min-w-[100px]"` on both the Cancel (ghost) and Submit (primary) footer buttons. Buttons auto-size to content with a minimum width to prevent collapse.

**QuizSection.tsx:**
- Replaced raw `<button>` for Finish Quiz with `<Button variant="primary" size="lg" loading={submitting} icon={<Trophy>} className="uppercase tracking-widest">`. The `loading` prop replaces the manual Loader2 ternary.
- Removed unused `Loader2` import.
- Removed `className="flex-1"` from Start Quiz Button.

## Deviations from Plan

**1. [Rule 1 - Bug] Removed unused Loader2 import from QuizSection**
- **Found during:** Task 2
- **Issue:** After replacing the raw Finish Quiz button, `Loader2` was imported but no longer used in the file
- **Fix:** Removed from the import block to prevent unused-import warnings
- **Files modified:** src/features/lessons/QuizSection.tsx
- **Commit:** 708f8fe

## Verification

- No `fullWidth` in any lessons feature file: confirmed
- No `flex-1` on SubmissionModal footer buttons: confirmed (remaining flex-1 in file is in attachment input area, not buttons)
- No `flex-1` on Start Quiz button: confirmed
- Finish Quiz uses `<Button>` component: confirmed (line 287)
- AssignmentHistoryCard submit uses `size="md"`: confirmed
- Build: Pre-existing Node 16 + Vite 6 `crypto.getRandomValues` failure (documented out-of-scope in STATE.md). TypeScript errors are all pre-existing TS2339/TS2792/TS1259 environment-level issues across the entire codebase, unrelated to these changes.

## Known Stubs

None.

## Self-Check: PASSED

- be2c4ec exists in git log: verified
- 708f8fe exists in git log: verified
- All 4 modified files exist and contain correct changes: verified
